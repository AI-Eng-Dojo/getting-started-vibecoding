# 講師・メンター用進行ガイド

参加者には配布しない。運営の判断基準と台本の要点をまとめる。

## 体制と機材

- 講師1名＋メンター（参加者8名につき1名目安）
- **講師・メンター機は全員Maxプラン必須**（救済ペア作業で枠を貸すため）
- 会場共有タイマー（後半の15分チェックポイント合図用）
- 配布する共有テンプレートリポジトリは無い。`vibecoding-0908`（前半2）・`myapp`（前半3〜後半）はいずれも**各参加者が当日その場でGitHub CLI（`gh`）を使って新規作成する**。事前準備0.4で `gh auth login` 済みであることが前提。`gh` がどうしても直らなかった参加者は、GitHub Web UIで手動作成→URLをClaude Codeに渡してcloneという代替手順に切り替えられる（01-part1・00-prep双方に記載）
- 置き場所は `~/github/`（Windowsは `C:\Users\⟨名前⟩\github\`）に統一する案内をしているが、**普段の置き場所がある参加者はそちらでも構わない任意扱い**。強制しない
- **手順上、リポジトリを作成した直後はまだ以前のフォルダを見ている。** 作成後に必ず「できたフォルダをVS Codeで開き直す」手順が挟まる（01-part1に明記）。ここを飛ばすと、Skillの保存先や以後のコミットが意図しない場所に散らばる。巡回時の最重要チェックポイント
- 提出フォーム3種: チェックポイント0（事前スクショ＋GitHubユーザー名＋`gh auth status` が通っている証跡）、チェックポイント3（`SKILL.md` 3つ＋`/tsumete` で質問が返る＋`CONTEXT.md`かADRのどちらかが実際に更新されている。**ADRが0本でも不合格ではない**）、宿題提出（後半前日までの `README.md`＋`TASKS.md`。書式は `templates/spec-readme.md`・`templates/tasks.md`）
- 救済版入り `starters/`（教材リポジトリ側。`starters/⟨題⟩/rescue/index.html`。配布URLは案内メールに記載）

## 設計思想（新任メンター向け）

1. **座学は合計20分以内。** 説明したくなったら「それはコラムに書いてある」と言って先へ進む
2. **タイトル回収は後半10〜11（レビュー→公開）。** 「動いて終わり」ではなく「その先まで見る」ことの証明はここ。時間が押しても**後半10の①〜③（2軸レビュー・セキュリティレビュー）**は絶対に切らない。後半11のデプロイは丸ごと切ってよいが、**公開するならステップ1（`public/` 分離）を飛ばさせない**（飛ばすと仕様書ごと公開される）
3. **戻ることを正規の技術として教える。** 救済版の利用やバックアップ復帰を「失敗」の空気にしない。メンターの声かけは常に「戻って進むのは今日教えた技です」
4. **利用枠はProで完走できる設計。** ただし作法（まとめ指示・当日温存）が守られている前提。乱れたら個別に軌道修正する
5. **前半は「体験させる」、後半は「一人でやりきらせる」。** 前半のGrill Me Skill自作でつまずいた参加者は、後半で必ず響く。前半3〜4の理解度は特に丁寧に見る

## 参加者構成と座席戦略（想定: 20名＝エンジニア10・非エンジニア（経営者・スタートアップ志望者など）10）

**テーブルは同質で分ける**（エンジニア島×2・非エンジニア島×2）。混成ペアにしない。

- 理由: 混成にするとエンジニアが隣のキーボードを「代わりに」触り始め、非エンジニアの手が止まる。本ワークショップの価値は自分の手で回した経験なので、これは最悪の失敗パターン
- メンター配分は非エンジニア島に厚く（非エンジニア島は5名に1人、エンジニア島は10名に1人目安）
- 交流は成果共有（後半13）で設計的に起こす。発表者はエンジニア・非エンジニアから最低1名ずつ選び、「同じプロセスで両方が形にできた」ことを見せるのがクロージングの説得力になる

進度差の吸収は🚀拡張ミッションが担う。エンジニアは本線を速く終えるので、拡張ミッション〔Eng〕（差分を読む・PRを出す・GitHub Actionsで自動デプロイ）へ誘導する。**本線の先回り（次のセクションを勝手に進める）は止める**——後続の説明が二度手間になるため。

> 注意: エンジニアはターン消費も速い。`/usage` 巡回では**エンジニア島の残量をむしろ警戒**する。
> 本ガイドが挙げる〔Eng〕3種（差分を読む・PRを出す・GitHub Actionsで自動デプロイ）はターン消費が少ない。ただし02-part2の「`/goal`で自走させる」「`prototype` で試作を作る」・01-part1の「もう1個作る」は消費が大きいので、残量が細い参加者には振らない。

## 待ち時間マイクロ解説ネタ集（コスト・ゼロの座学枠）

参加者は生成待ちで合計アイドル時間が出る。ハンズオン中、会場全体が「待ち」に入った瞬間に1枚スライドで挟む。**1本90秒以内・手を動かし始めた人がいたら即打ち切り**が鉄則。

| # | ネタ | 差し込みどころ | 要点 |
|---|---|---|---|
| W1 | いまClaudeは何をしているか | 前半①の最初の生成待ち | 指示→計画→ファイル操作→確認のループを回している。画面のツール実行表示の読み方 |
| W2 | 権限プロンプトの裏側 | 前半①で権限質問が出た頃 | **参加者テキストに掲載済み**（01-part1「『許可しますか?』と聞かれたら」）。今日はAuto Modeなので確認自体が少ないことが前提。口頭では「出たときは、AIが判断を委ねてきたサイン。2番は記憶される」の一点だけ補強する |
| W3 | まとめ指示が効く本当の理由 | 前半①のまとめ指示後 | ターン数節約だけでなく、方向性が一度に伝わるので手戻りが減る |
| W4 | Skillはなぜ効くのか | 前半②のtsumete作成中（ハンズオン①） | プロンプトは消えるがSkillは残る。「毎回言っていること」を資産化する発想 |
| W5 | descriptionが呼ばれる/呼ばれないの分かれ目 | 前半②のtsumete動作確認時（ハンズオン①） | Claudeはdescriptionだけを見て使うか判断している。**症状別の直し方は参加者テキストの表（前半4・ハンズオン③）にあるので、そちらを見させる**。口頭は「自分が実際に打つ言葉をdescriptionに入れる」の一点 |
| W6 | 利用枠の仕組み | 後半開始直後の `/usage` 時 | 5時間ウィンドウ＋週次の二重構造。まとめ指示が枠の節約にもなっている |
| W7 | 「幻覚したgreen」実例 | 後半⑥の中盤の生成待ち | 「できました」と言って確認していなかった実例を1枚見せる。10の伏線 |
| W8 | Skillをチームに配るという発想 | 前半④の待ち | 個人の作法をリポジトリに入れるとチームの標準になる。拡張ミッションへの布石 |
| W9 | なぜHooksは確実に動くのか | 前半④のHook設定中 | SkillはAIが「使うか」を毎回判断するが、Hooksはイベント条件に合致すれば確認なしに必ず実行される。だからこそ登録は信頼できる内容だけに、という安全側の話に必ずつなげる |

運用メモ: 全部やる必要はない。**W2・W3・W7は必須**（安全・作法・検証の3本柱）、残りは会場の待ちの発生に合わせて取捨。

## 事前準備期間の運営

- チェックポイント0の提出状況を前日夜に集計。未提出者には個別連絡＋当日30分前のサポートタイム招集
- 提出スクショで環境の偏り（VS Code拡張／CLI、OS）を把握し、メンター配置に反映
- **チェックポイント0で `gh auth status` の実行結果（スクリーンショットでよい）を提出させ、前日夜に全員分そろっているか確認する。** `gh` 未認証のまま当日を迎えると、ハンズオン①冒頭の `gh repo create` で全員が止まる。未提出・未認証者は当日朝のサポートタイムで潰す
- **`gh` がどうしても使えない人は、GitHub web UIの「New repository」から手動作成し、Claude Codeに `git clone` してもらう代替手順がある**（00-prep.md 0.4に記載）。前日夜の確認で `gh auth status` が通っていない人には、この代替手順も案内しておく
- **救済版は開催前に中身を見直す。** `main` 上の `starters/⟨題⟩/rescue/index.html` が実体で、**Gitタグは使わない**（タグ名と文書中のURLを人手で同期する方式は一度壊れた実績があるため）。開催前に各お題を実際に完成まで作り込んで差し替え、`.github/workflows/link-check.yml` が緑であることを確認する
- **Windows参加者は既知の環境問題が2つある**（00-prep.md 0.2に対処法を記載済みだが、提出スクショや当日の様子で見落としが疑われたら能動的に確認する）
  - Node.js未インストールだとClaude Code拡張がそもそも起動しない（Windows版のみの制約。Anthropicは対応しない方針で恒久的）
  - 拡張が特定バージョンで起動時にクラッシュするビルド不具合が断続的に再発している（2026年2月以降、複数バージョンで報告あり。Node.jsを入れても直らない場合はこちら）
  - **復旧策は症状で分かれる。** ①起動しない・反応しない＝Node.js未インストール→nodejs.orgのLTS版を入れてPC再起動（00-prep 0.2の3）。これで拡張自体が起動するので、CLI版への切り替えは不要。②Node.jsを入れているのに起動時クラッシュ（TypeErrorなど）＝既知のビルド不具合→VS Codeのターミナルで `npm install -g @anthropic-ai/claude-code` → `claude` コマンドでCLI版に切り替え（npmはNode.js同梱のため、Node.js導入済みが前提）。**拡張のUIが使えないだけで、今日の内容はCLI版でも支障なく進められる。** メンター側もこの2分岐と切り替え手順を把握しておく

## 前半の進行キュー

| 時間（案） | セクション | 講師の要点 | メンターの動き |
|---|---|---|---|
| 0:00 | 1. オープニング | 今日の全体地図（前半→宿題→後半）を映しっぱなしにする。最後に**`/help` を投影で1回見せてから、全員で `/status` → `/model` → `/usage` を叩かせる**（`/effort` は任意、押していれば飛ばす）。前半2は軽いモデルで十分＝枠が持つ、を口頭で | 巡回して`/model`の選択で固まっている人を拾う。残量が少ない人を把握 |
| 0:10 | 2. ハンズオン①（65分） | **Vibe Coding→MCP→Agent Skillの3本柱を通しで触らせる。** 完成度を求めない。「頼んだら動いた」の驚きを優先させる。**「作る→見る→直す」(15分)のあと、MCP/Gmailハンズオン(20分)**——claude.aiでGmailコネクタを接続させ、下書きメールを1通作らせる。**接続後は`/clear`ではなくClaude Codeの再起動が必要**であることを口頭で明確に伝える。個人のGmailを使うことに抵抗がある人は無理に接続させず、隣の画面を見学でも可。**続けて `tsumete` の最初のSkill作成(20分)**——一問一答の簡易版でよい、`.claude/skills/`（プロジェクトレベル）に保存させ `/tsumete` で動くところまで確認させる。**`disable-model-invocation: true` は①から入れさせる（呼び出しは最初から最後まで `/tsumete` で統一）。**そのあと**残り8分で全員に `/permissions` と `/config` を開かせる**（余裕があれば `/context` も）。最後に `/clear` を必ず全員に。**予備2分** | 権限プロンプトで固まる人・`gh repo create` で認証エラーになる人を最優先で拾う。**claude.aiでコネクタは接続済みなのにClaude Codeから使えない人は、ほぼ全員「再起動していない」が原因。** `/clear`だけして再挑戦している参加者を見つけたら、Claude Codeそのものを終了→再度開かせ、`/mcp` でConnectedを確認させる。Google側のOAuth画面で止まる人（2要素認証・会社アカウントのポリシー制限）を優先的にケアし、詰まったら見学に切り替えさせる。「2番を読まずに連打」を見つけたら止める。**Auto Modeで確認自体が少ないぶん、逆に「何も起きていないように見えて不安」と申告する人が出やすい。画面のツール実行表示を一緒に見て、実際にファイルが動いていることを確認させる** |
| 1:15 | 3. ハンズオン②（30分） | 壁打ちは**コードを書かせず**対話に集中させる。「アイデアがない」参加者はここでメンターが直接介入 | アイデアが出ない参加者への種まき質問（最近の面倒ごと・繰り返している作業）を用意しておく。**`myapp` を作った直後、`tsumete` が「呼ばれない」ことを体験させる設計になっている。** ここで慌ててプロジェクトレベルにコピーさせるだけで終わらせず、**ユーザーレベル（`~/.claude/skills/`）に置き直すところまで**必ずやらせる——宿題・後半でずっと使うのはこちらのコピー |
| 1:45 | 休憩（15分） | 3時間の折り返し地点。前半最長の枠（65分のハンズオン①）はもう終えている。次のハンズオン③（35分）に備えて一度離席させる | 巡回不要。この間に前半4提出物（チェックポイント3）の確認体制を確認しておく |
| 2:00 | 4. ハンズオン③（35分） | **`tsumete`（①で作成済み）を、`tsumetsume`・`ddd` に役割を分けて育てさせる。** `tsumetsume`・`ddd` は新規作成、`tsumete` は更新。descriptionの書き方が肝であることを必ず口頭で。1つ目（tsumetsume）は自分の言葉で書かせ、ddd・tsumete更新は雛形寄りでよいと伝えて時間を守る。**`tsumete` の `disable-model-invocation: true`（①から入っている）の意味——中身を持つスキルは自動で呼ばれてよい／入口は人間が明示的に呼ぶ——を、3つに分けたこの場で必ず口頭で補強する。`tsumetsume`・`ddd` にはこの行を入れさせない** | Skillが呼ばれない参加者の多くはdescription不足。一緒に読んで直させる。`tsumete` の参照名ズレ（保存した名前と本文中の名前が違う）にも注意。**すでにユーザーレベルにある `tsumete` を上書き更新している**ことを見失っている参加者がいたら、置き場所（②で学んだ内容）を思い出させる。**「『詰めて』と書いたのに動かない」は仕様どおり。`/tsumete` に切り替えさせる（①から一貫してこの呼び方）** |
| 2:35 | 5. ハンズオン④（20分） | **Hooksのハンズオンは必修。** 「Skillは呼ばれないことがある／Hooksは条件が合えば確実に動く」の対比を必ず口頭で。ログファイルに1行増えるのを自分の目で見せてから次へ。**Security Review Skillはここでは扱わない（後半10で完成例をコピーする）**——時間が余ったら「他にどんなSkillが作れそうか」のアイデア出しに使わせる | Hookが反映されない参加者は `/hooks` で登録内容を確認させ、入っているのに動かない場合は**Claude Codeを一度終了して開き直させる**（`/clear` は本文の手順で既に済ませているので効かない） |
| 2:55 | 6. まとめ・宿題説明 | 宿題の締切と提出先を明言する。**制約3点（ブラウザだけ・外部連携なし・実装70分に収まる規模）を読み上げる。** 「外部連携なし」と後半11のデプロイが矛盾しないこと（変わるのは置き場所だけ）も一言添える | 宿題着手済みの参加者から先に声をかけ、着手方法を隣に共有させる |

## 宿題期間の運営

- 宿題提出は **`README.md` と `TASKS.md` の2ファイル**。後半開始の**前日中**を締切とする
- **チェックポイント3（3つの `SKILL.md` の中身。`~/.claude/skills/` 配下でリポジトリの外にあるため、フォームは貼り付け or アップロードで受け取る）は前半終了後〜当日夜に、担当メンター1名を決めて目を通す。**（前半の休憩は1:30〜1:45の1回のみで、ハンズオン③(提出物が出るタイミング)より前にある。チェックポイント3の提出は2:35頃なので、その後にまとまった休憩は無く、実運用はこのタイミングになる）
  - 見るのは3点: **`tsumetsume`・`ddd` の** `description` に「いつ使うか」が入っているか（`tsumete` は `disable-model-invocation: true` なので呼び出し語は不要）／`tsumete` 本文が参照するSkill名が保存名と一致しているか／`tsumete` 末尾にREADME.md・TASKS.md書き出しの指示があるか
  - 3点目が無い人には宿題着手前に連絡するのが理想だが、6終了直後に着手する参加者もいて間に合わないことがある。**取りこぼしは後半8冒頭のTASKS.md生成プロンプト（02-part2 §7）で回収できるので必須ではなく「できれば前倒し」**
- メンターは提出物に目を通し、次のいずれかに該当するものをリストアップしておく
  - **`TASKS.md` が無い**: 自作 `tsumete` に書き出しの指示を入れ忘れている。後半8の冒頭で作らせれば追いつく（02-part2の§7表の下にあるプロンプトを使う）
  - **大きすぎる**: 後半の実装70分で終わらない規模。**ただし「やることが多い」こと自体は問題ではない**（後半8で線を引く）。問題なのは1つ1つが大きい場合
  - **曖昧さが残っている**: 「使いやすく」など目で確認できない表現がまだある
  - **制約違反**: ログイン・サーバー・外部サービス連携が「できること」に残っている。**後半8で発覚すると実装70分が崩れるので、前日中に必ず潰す**
  - **未提出**: 後半7（宿題ふりかえり）で個別フォローが必要
- 該当者には後半開始前、または7の時間に声をかける。頭ごなしに直させず、「今日の実装70分で終わる形」に絞る手伝いをする

## 後半の進行キュー

| 時間（案） | セクション | 講師の要点 | メンターの動き |
|---|---|---|---|
| 0:00 | 7. 宿題ふりかえり | **全員に `myapp` のフォルダを開かせるところから。** `/usage` 巡回。手元にあるものを3段階で自己判定させる。**「Cloudflareアカウントは9の生成待ち時間に作っておいて」を全体アナウンス** | 宿題リストの分類（TASKS.md欠落／大きすぎ／曖昧／制約違反／未提出）を個別に回る |
| 0:05 | 8. ハンズオン⑤ 線引き | **冒頭でまず「TASKS.mdが無い人は§7のプロンプトを貼って」と全体アナウンス。** そのあとステップ1（粒度を揃える・5分）→ステップ2（4つを選ぶ・10分）の順。**「今日やる4つは、それだけで動くものが1本通る組み合わせ」を必ず一度言わせる。** 上から4つではない | 4つに絞れない参加者を手伝う。迷ったら減らす方へ倒させる |
| 0:20 | 9. ハンズオン⑥ 実装 | **冒頭5分で `tdd` スキルをコピーさせ、`/clear` まで全員そろえてから開始。** 以降15分ごとに合図＋**「目印は確認した? コミットした? プッシュした? TASKS.mdに反映した?」**のコール。**目印を見ずにコミットさせない。**「Claudeが『できました』と言っても、それは終わったという意味ではない」を一度言う | 巡回。挙手対応はエスカレーション順を守らせる（いきなり答えを教えない）。**この70分はファイルを一切手で編集させない**（コードもTASKS.mdもClaude Codeに書かせる）のが前提。**`tdd` が呼ばれず目印の確認を飛ばしていきなり実装している参加者**には、02-part2 §9の「スキルが呼ばれないときは」の手動プロンプトを案内 |
| 1:30 | 10. ハンズオン⑦ レビュー | **冒頭で「『レビューして』は1つの質問ではない」を必ず言う。** ①スキル2枚コピー（6分）→②2軸レビュー＋修正（12分）→③セキュリティ（4分）→④Hook（15分）。**順番厳守。** ②で**Spec軸が宿題の `README.md` を読む**のがこの日の隠れた回収点なので、ここは口頭で拾う。「できました」を鵜呑みにしない・最終権限は自分の目、を繰り返す | 指摘が1本のリストで返ってきた参加者には「2つの軸に分けて報告し直して」と言わせる。**Hookが動かない人は深追いさせず、①〜③だけで11へ送る**（完成判定に影響しない） |
| 2:07 | 11. ハンズオン⑧ デプロイ | **ステップ1（`public/` 分離）を絶対に飛ばさせない。** 飛ばすと仕様書・TASKS.md・`.claude/` ごと公開される。公開は**ルートA（Cloudflare Drop・ブラウザだけ）が全員の本線**、ルートB（wrangler）はNode.jsがある人のみ。**スマホで開かせるところまでが11** | Node.jsが無いのにルートBに行こうとする人をAへ誘導。ルートBの人が `wrangler deploy` でHookに止められたら**それが正常**だと伝える（`wrangler.jsonc` の書き込みでHook Aが印を消すため）|
| 2:27 | 12. ハンズオン⑨ MCP | **NotionかBacklogのどちらか片方でよい**と最初に言う（迷ったらNotion）。**Claude Codeの再起動が必要**を全体アナウンス。**「TASKS.mdを置き換えるのではなく出口を1本つけるだけ」**を必ず言う | **Backlogを選んだ人のスコープを必ず目視確認する。`project` スコープはAPIキーが公開リポジトリに乗る事故**。Node.jsが無い人はNotionへ。接続が通らない人は手順を見るだけで可 |
| 2:42 | 13. 成果共有 | 発表者は9の巡回中に目星を付けておく（エンジニア1・非エンジニア1・意外なアイデア1）。**公開URLがある人は画面に出させる** | 発表者の画面共有段取り |
| 2:49 | 14. クロージング | 一連の流れの図で締める。**つないだコネクタ（Gmail・Notion）の外し方が資料にあることを口頭で1回示す**（外すこと自体は推奨しない。場所を知らせるだけ）。**「Skillに数行足しただけで宿題の持ち物が変わった」**と**「工程を小さく分けて噛み合わせた」**の2つを最後に言う。Maxアップグレードの案内は「枠が足りなかった人向け」の実務トーンで | — |

## 時間が押したときの切り方（優先順）

1. 前半5のSecurity Review Skillを参加者に作らせず、講師が画面共有して眺めるだけにする（**Hooksのハンズオン自体は必修のため完全カットはしない**。「他にどんなSkillが作れそうか」は01-part1側で「時間が余ったら」扱いなので、押している時点で自然に消える＝別途打ち切る必要はない）
2. 前半4で参加者が大きく遅れている場合、`ddd` は「答え合わせ用の完成例を眺めるだけ」に切り替えさせ、`tsumete` は `tsumetsume` だけを参照する簡易版（1問1答のみ、書き残しは無し）で妥協させる。**tsumetsumeを自分の言葉で書く体験だけは死守する。** この場合、チェックポイント3は「`SKILL.md` 2つ＋`/tsumete` で質問が返る」までで可と全体アナウンスし、`CONTEXT.md`/ADRの確認要件は外す。ただし `tsumete` 末尾のREADME.md・TASKS.md書き出し指示だけは簡易版にも必ず入れさせる（宿題の持ち物に直結するため）
3. **後半12（MCP連携）を全カットして13へ飛ばす。** 後半でいちばん切りやすい。「今回やらないこと」は `TASKS.md` に残っているので、持ち帰りは成立する
4. 後半13の発表を3名→2名に減らす
5. 前半2の「設定を開いて中を見る」を、`/permissions` と `/config` の2つだけに絞る（`/context` は飛ばし、`/clear` は必ずやらせる）
6. **切ってはいけないもの**（時間が無いときの調整はここまで。以下は削らない）
   - 後半10の①〜③（2軸レビューとセキュリティレビュー）。**④のHookは切ってよい**
   - 後半11のステップ1（`public/` 分離）。**ここを飛ばすと仕様書・`.claude/` ごと公開される。デプロイ自体を切るのは可だが、分離を飛ばして公開させるのは不可**
   - 前半4のtsumetsume自作（アイデア言語化＋Grill Me Skillの核）
   - 前半5のHooksハンズオン。**参加者が自分でHookを設定しログ1行を確認するところまでが必修＝チェックポイント4。** 時間が無いときは講師デモを先に見せて設定作業を短縮してよいが、**デモだけで終わらせない**
   - 後半9の15分チェックポイント運用
   - 後半8の線引き（飛ばすと実装が終わらない）

## トラブル対応表

| 事象 | 対応 |
|---|---|
| 利用枠の上限到達（Pro） | メンター機（Max）でペア作業。本人に操作させ、メンターは枠を貸すだけ。5時間ウィンドウのローリング回復も案内 |
| Gmailコネクタの接続でOAuth画面が進まない・2FAで止まる | 個人のスマホでの2FA完了を優先してもらう。時間がかかりそうなら先に進ませ、隣の画面を見学する側に回す |
| 会社支給のGoogleアカウントで接続が拒否される（組織ポリシー） | 個人アカウントへの切り替えを勧める。個人アカウントも持っていない・作りたくない場合は接続せず見学に回す（本線への影響なし） |
| コネクタを接続したのにClaude Codeから使えない | **`/clear` では直らない。Claude Codeを再起動（終了→再度開く）しているか確認。** MCPの接続はプロセス起動時に確立されるため、会話を切っただけでは反映されない。再起動後は `/mcp` でConnectedになっているか確認させる |
| （Windows）拡張が起動しない・反応しない | Node.js未インストールが原因。LTS版を入れてPC再起動させる（00-prep.md 0.2の3）。CLI版への切り替えは不要 |
| （Windows）拡張がTypeErrorなどでクラッシュする | 既知のビルド不具合。VS Codeのターミナルで `npm install -g @anthropic-ai/claude-code` → `claude` でCLI版に切り替えさせる（Node.js導入済みが前提。00-prep.md 0.2参照） |
| 全体的に応答が遅い（障害・混雑） | ハンズオンは待ち時間に言語化ワーク・仕様の見直しを前倒しする |
| 環境が完全に壊れた参加者 | **先にいまの状態をコミット＆プッシュ（またはファイルをコピー退避）させてから**、Claude Code Web版（ブラウザ実行）に切り替えて継続。自作Skillは `~/.claude/skills/` 直下でリポジトリの外にあるため引き継がれない。`SKILL.md` の中身を貼り直させる |
| 後半9でプッシュが弾かれる（コミットは通るが push だけ失敗する） | `gh auth status` を確認させる。**未認証・別アカウントでログインしている人**→ブラウザでGitHubログインを通し直させる。**そもそも `myapp` を作れていない人**→ハンズオン②の手順に戻ってその場で作り直す（`gh`がだめならWeb UI手動作成＋clone） |
| Skillが作ったはずなのに呼ばれない・pushしたファイルが想定と違う場所にある | **リポジトリ作成直後にVS Codeを開き直していない可能性が高い。** Claude Codeがまだ1つ前のフォルダを見ている状態で作業を続けている。`~/github/vibecoding-0908`（または `myapp`）を直接開き直させる |
| アイデア乗り換えを始めた参加者 | 「メモして持ち帰る」に誘導。乗り換えは迷子の最大要因という理屈まで伝える |
| 宿題が未着手のまま後半に来た参加者 | 前半2〜3で作ったものを種に、後半7の時間で急いで1周させる。それでも決まらなければ [starters/](../starters/) のお題を仕様として仮採用させる |
| やることが20個ある参加者 | **減らさせない。** 後半8で今日やる4つに線を引き、残りは「今回やらないこと」へ移させる。詰めた量は持ち帰る資産だと伝える |
| 後半10で指摘が1本のリストで返ってくる | 「仕様どおりか」と「作法どおりか」の2つの見出しに分けて報告し直させる。**混ざったリストは、どれが重いか分からなくなる**というのがこのハンズオンの主題そのもの |
| 後半11で `wrangler deploy` がHookに止められる | **正常な挙動。** `wrangler.jsonc` を書いた時点でHook Aが印を消しているため。`セキュリティレビューして` を通してからデプロイを頼み直させる |
| Cloudflare Dropで公開したURLが消えたと言われる | **アカウント未Claimなら1時間で消える仕様。** 残したい人はその場で無料登録してClaimさせる。消えていても完成判定には影響しない |
| 後半12でBacklog MCPが接続できない | `node -v` が通らない人はNode.js未導入（Mac勢に多い）。**Notionへ切り替えさせる。** APIキーの再発行を試す前にここを確認する |
| 早く終わった参加者 | 該当ハンズオンの🚀拡張ミッションへ誘導（エンジニアは〔Eng〕系）。本線の先回りは止める。メンター補助役を頼むのも可 |

## 実測データの収集（次回改善用）

各回で記録する:

- チェックポイント通過率（0〜7の各段階）
- 宿題提出率と、提出物の分類（TASKS.md欠落／大きすぎ／曖昧さ残り／制約違反／未提出。「宿題期間の運営」の5分類に準拠）の内訳
- 上限到達者数とその時刻・プラン内訳
- 参加者の平均ターン数の聞き取り（任意アンケート）
- 詰まりポイントのカテゴリ集計（環境／git・保存（gh認証/repo作成/push）／指示／Skill作成／宿題）
- 前半1・2・4の実所要時間（10分／65分／35分の配分が妥当だったか。ハンズオン①へのtsumete作成追加＋Grill Me Skillの3スキル化＋2時間→3時間化後の初回計測）
- MCP/Gmailハンズオン（前半2内、目安20分）の実所要時間と、コネクタ接続でつまずいた人数・原因（OAuth・2FA・組織ポリシー・個人アカウント忌避で見学に回った人数）
- 宿題提出物のうち「制約違反」（ログイン・サーバー必須）の件数

> ワークショップは実測で改訂する。アンケートに「宿題の負担感」を1問入れる（前半→後半の構成自体が今回の変更点のため）。
