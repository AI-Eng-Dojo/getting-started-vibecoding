# 講師・メンター用進行ガイド

参加者には配布しない。運営の判断基準と台本の要点をまとめる。

## 体制と機材

- 講師1名＋メンター（参加者8名につき1名目安）
- **講師・メンター機は全員Maxプラン必須**（救済ペア作業で枠を貸すため）
- 会場共有タイマー（後半の15分チェックポイント合図用）
- 配布する共有テンプレートリポジトリは無い。`vibecoding-0908`（前半2）・`myapp`（前半3〜後半）はいずれも**各参加者が当日その場でGitHub CLI（`gh` コマンド）を使って新規作成する**。事前準備0.4で `gh auth login` 済みであることが前提。`gh` コマンドがどうしても直らなかった参加者は、GitHub Web UIで手動作成→URLをClaude Codeに渡してcloneという代替手順に切り替えられる（01-part1・00-prep双方に記載）
- 置き場所は `~/github/`（Windowsは `C:\Users\⟨Windowsのユーザー名⟩\github\`）に統一する案内をしているが、**普段の置き場所がある参加者はそちらでも構わない任意扱い**。強制しない
- **手順上、リポジトリを作成した直後はまだ以前のフォルダを見ている。** 作成後に必ず「できたフォルダをVS Codeで開き直す」手順が挟まる（01-part1に明記）。ここを飛ばすと、エージェントスキルの保存先や以後のコミットが意図しない場所に散らばる。巡回時の最重要チェックポイント
- 提出フォーム3種: チェックポイント0（事前スクショ＋GitHubユーザー名＋`gh auth status` が通っている証跡）、チェックポイント3（`SKILL.md` 3つ＋`/tsumete` で質問が返る＋用語や決定が確定した場合に`CONTEXT.md`かADRへ記録される。**確定事項がなければどちらも0件でよい**）、宿題提出（後半前日までの `README.md`＋`TASKS.md`。書式は `templates/spec-readme.md`・`templates/tasks.md`）
- 救済版入り `starters/`（教材リポジトリ側。`starters/⟨題⟩/rescue/index.html`。配布URLは案内メールに記載）
- **講師の投影用環境は、参加者と同じまっさらな状態にする。** 自分用のエージェントスキル（`~/.claude/skills/`）や設定が入ったまま投影すると、同名の `tsumete` が候補に2つ並ぶ、別のエージェントスキルが勝手に動いて計画を立て始める、といった参加者の画面と違う動きになる。特に前半3の「`myapp` では `tsumete` が呼ばれない」は、講師機に同名のものがあると再現できない。投影専用のOSユーザーを用意するなど、既存の設定を読まない状態で臨む

## 設計思想（新任メンター向け）

1. **座学は最小限にする。** 説明したくなったら「それはコラムに書いてある」と言って先へ進む
2. **実装前にレビューを用意し、完成判定まで人間が確かめる。** 後半9のコミット前レビューと後半10の通し確認、初回プッシュ前のセキュリティレビューは省かない。後半11を実施するなら、公開用の `public/` 分離と公開設定の確認を飛ばさせない。
3. **戻ることを正規の技術として教える。** 救済版の利用やバックアップ復帰を「失敗」の空気にしない。メンターの声かけは常に「戻って進むのは今日教えた技です」
4. **利用枠はProで完走できる設計。** ただし作法（まとめ指示・当日温存）が守られている前提。乱れたら個別に軌道修正する
5. **前半は「体験させる」、後半は「一人でやりきらせる」。** 前半のGrill Me系エージェントスキル自作でつまずいた参加者は、後半で必ず響く。前半3〜4の理解度は特に丁寧に見る

## 参加者構成と座席戦略（想定: 20名＝エンジニア10・非エンジニア（経営者・スタートアップ志望者など）10）

**テーブルは同質で分ける**（エンジニア島×2・非エンジニア島×2）。混成ペアにしない。

- 理由: 混成にするとエンジニアが隣のキーボードを「代わりに」触り始め、非エンジニアの手が止まる。本ワークショップの価値は自分の手で回した経験なので、これは最悪の失敗パターン
- メンター配分は非エンジニア島に厚く（非エンジニア島は5名に1人、エンジニア島は10名に1人目安）
- 交流は成果共有（後半13）で設計的に起こす。発表者はエンジニア・非エンジニアから最低1名ずつ選び、「同じプロセスで両方が形にできた」ことを見せるのがクロージングの説得力になる

進度差の吸収は🚀拡張ミッションが担う。エンジニアは本線を速く終えるので、拡張ミッション〔Eng〕（差分を読む・PRを出す・GitHub Actionsで自動デプロイ）へ誘導する。**本線の先回り（次のセクションを勝手に進める）は止める**——後続の説明が二度手間になるため。

> 注意: エンジニアはターン消費も速い。`/usage` 巡回では**エンジニア島の残量をむしろ警戒**する。
> 本ガイドが挙げる〔Eng〕3種（差分を読む・PRを出す・GitHub Actionsで自動デプロイ）はターン消費が少ない。ただし02-part2の「`/goal`で自走させる」「`prototype` で試作を作る」・01-part1の「もう1個作る」「メールの返信漏れを探す」は消費が大きいので、残量が細い参加者には振らない。

## オンライン開催の運営

会場開催を前提にした上の座席戦略は、オンライン（Discordなどの音声チャンネル）では次のように読み替える。

- **詰まった人は、声かチャットで申告してもらう。** 講師は全体を止めず、メンターが個別の音声チャンネルに移って画面共有で対応する。「自分の環境だけかもしれないから言わない」を防ぐため、冒頭で「詰まって申告してもらうのがメンターの仕事」と明言する
- **講師が待つのは、全員に共通する手順（リポジトリ作成など）で遅れが出たときだけ。** 待つ間は、下の待ち時間ネタから教材とつながるものを話す。「本編と関係ない話」で埋めない
- **合流したかは、個別チャンネルから戻ったメンターが講師に口頭で伝える。** 戻ってきた参加者に「どこまで進んだか」を講師から確認し、次の手順に移る
- **呼び方は「メンター」に揃える。** 教材も本ガイドもメンターで書いているので、当日の口頭でも言い換えない
- 後半の15分チェックポイントの合図は、会場タイマーの代わりに画面共有にタイマーを出し、チャットでも一言流す

## 待ち時間マイクロ解説ネタ集（コスト・ゼロの座学枠）

参加者は生成待ちで合計アイドル時間が出る。ハンズオン中、会場全体が「待ち」に入った瞬間に1枚スライドで挟む。**1本90秒以内・手を動かし始めた人がいたら即打ち切り**が鉄則。

| # | ネタ | 差し込みどころ | 要点 |
|---|---|---|---|
| W1 | いまClaudeは何をしているか | 前半①の最初の生成待ち | 指示→計画→ファイル操作→確認のループを回している。画面のツール実行表示の読み方 |
| W2 | 権限プロンプトの裏側 | 前半①で権限質問が出た頃 | **参加者テキストに掲載済み**（01-part1「『許可しますか?』と聞かれたら」）。今日はAuto Modeなので確認自体が少ないことが前提。口頭では「出たときは、AIが判断を委ねてきたサイン。2番は記憶される」の一点だけ補強する |
| W3 | まとめ指示が効く本当の理由 | 前半①のまとめ指示後 | ターン数節約だけでなく、方向性が一度に伝わるので手戻りが減る |
| W4 | エージェントスキルはなぜ効くのか | 前半②のtsumete作成中（ハンズオン①） | プロンプトは消えるが、保存した作法は残る。「毎回言っていること」を資産化する発想 |
| W5 | descriptionが呼ばれる/呼ばれないの分かれ目 | 前半②のtsumete動作確認時（ハンズオン①） | Claudeはdescriptionだけを見て使うか判断している。**症状別の直し方は参加者テキストの表（前半4・ハンズオン③）にあるので、そちらを見させる**。口頭は「自分が実際に打つ言葉をdescriptionに入れる」の一点 |
| W6 | 利用枠の仕組み | 後半開始直後の `/usage` 時 | 5時間ウィンドウ＋週次の二重構造。まとめ指示が枠の節約にもなっている |
| W7 | 「できました」の確かめ方 | 後半⑥の中盤の生成待ち | 完了定義を読み違えたまま実装できてしまう。同梱のコードレビューも欲しかった体験かは判定しないので、人間が画面で確かめる。任意の自動テストを使う場合も同じ |
| W8 | エージェントスキルをチームに配るという発想 | 前半④の待ち | 個人の作法をリポジトリに入れるとチームの標準になる。拡張ミッションへの布石 |
| W9 | Hooksが動く条件 | 前半④のHook設定中 | 登録したイベントで処理を呼ぶ仕組み。Claude Code外からの操作や設定対象外のコマンドには動かない。後半9ではレビュー結果が届くまで待つ必要も説明する |
| W10 | `CLAUDE.md` は毎回読まれる | 前半①でリポジトリ作成に遅れが出て、全体が待つとき | 前半3で `myapp` に置く `CLAUDE.md` の予告。会話を始めるたびに丸ごと読まれるので、長くするほど毎回のコンテキストを圧迫する。フロントエンドとバックエンドが同居するリポジトリなら、片方の開発ルールをもう片方の人にまで毎回読ませない工夫が要る。詳しくはコラム★3へ誘導する |

運用メモ: 全部やる必要はない。**W2・W3・W7は必須**（安全・作法・検証の3本柱）、残りは会場の待ちの発生に合わせて取捨。

## 事前準備期間の運営

- チェックポイント0の提出状況を前日夜に集計。未提出者には個別連絡＋当日開始前のサポートタイム招集
- 提出スクショで環境の偏り（VS Code拡張／CLI、OS）を把握し、メンター配置に反映
- **CLIも全員が事前に導入する。** 00-prep.md 0.2の5に従い、ターミナルで `claude --version` の表示と `claude` の起動を確認させる。拡張に同梱されるCLIとは別に、ターミナル用の導入が必要。WindowsではPowerShellを選ばせる。通常の操作は引き続きVS Code拡張で進める
- **チェックポイント0で `gh auth status` の実行結果（スクリーンショットでよい）を提出させ、前日夜に全員分そろっているか確認する。** `gh` コマンド未認証のまま当日を迎えると、ハンズオン①冒頭の `gh repo create` で全員が止まる。未提出・未認証者は当日朝のサポートタイムで潰す
- **`gh` コマンドがどうしても使えない人は、GitHub web UIの「New repository」から手動作成し、Claude Codeに `git clone` してもらう代替手順がある**（00-prep.md 0.4に記載）。前日夜の確認で `gh auth status` が通っていない人には、この代替手順も案内しておく
- **Windows版拡張の起動条件は[検証メモ](windows-extension-verification.md)を参照する。**
  2026-09-08のCIでは、Windows Server 2025で外部Node.jsをPATHから除外し、外部 `node.exe` を起動せずに、検証した版の拡張でログイン画面まで表示できた。
  物理Windows 10/11と認証後の動作は未検証。
  講座では後半9のレビューフック、任意のテスト（Playwright）、後半11の `npx wrangler` による公開、後半12のBacklog連携に使うため、引き続き全OSでNode.jsを導入する。
- **Windowsで、後半7の見本展開コマンド（PowerShell版: `curl.exe` で `main.zip` を保存して `tar -xf --strip-components=4`）が通ることを開催前に1回確認する。** macOS版（`curl | tar -xz`）は2026-09-11に main に対して動作確認済み。Windows版は同じ `tar`（bsdtar）がzipを展開できる前提で書いており未検証。通らなければ、本文の「手で写す」（ZIPを手で展開）へ誘導する
- **VS Code拡張で `/context` の Memory files に `myapp` の `CLAUDE.md` が出ることを開催前に1回確認する。** `docs/01-part1.md` 前半3は、最初に置いた `CLAUDE.md` の読み込み確認に `/context` を使わせている。根拠は公式のメモリのページ（英語版。「読み込みの確認は `/context` の Memory files で行う。`/memory` は未作成のファイルも候補として並べる」、2026-09-08確認。日本語版は古く `/memory` で確認すると書いてある）。拡張のUI上での見え方は未検証。出なかった場合の手当て（Claude Codeを終了して開き直す）は本文に入れてある
- **Backlog MCPのバージョンを開催前に確認する。** `docs/02-part2.md` は `backlog-mcp-server@0.18.0` を固定で指定している（2026-08-30 時点の最新）。開催前に、その版が現在も取得できること・想定どおり動くことを確認し、必要なら番号を更新する。**バージョンを外して最新追従にしないこと**——当日何が動くか分からなくなる
- **新しく作ったCloudflareアカウントで、後半11を開催前に1回通す。** 公開の手段は `wrangler` だけになった。作ったばかりのアカウントでは、`wrangler login` の途中の「Sign in with Google」からログイン画面へ戻れるか、初回デプロイでサブドメインの自動登録に失敗したとき、本文のプロンプトどおりClaudeが止まってURLを示すか、そのURLの画面で名前を登録できるかが未検証。自動登録の挙動は `wrangler` 4.131.2 のソースで確認した（アカウントにworkers.devのサブドメインが無く、Claude Codeの中で動いていると、確認を出さずに `package.json` の `name` かフォルダ名、つまり全員 `myapp` で登録しようとして、名前が使えないエラーで止まる）。`login` → `whoami` → `deploy` まで通し、`.wrangler/` ができていればコミット対象に入っていないことも `git status` で確かめる（`node_modules/` があるとキャッシュはそちらに置かれ、`.wrangler/` はできない）
- **任意のTDD課題を案内する場合は、Playwrightを事前検証する。** `skills/tdd/SKILL.md` の固定版で準備し、テストが落ちる→通るまで一件を回す。番号を変える場合は配布物と本文をまとめて更新する。
- **後半9のフックを開催時のClaude Codeで実行確認する。** 同梱 `/code-review` の結果受領、コード変更後の一度停止、毎回のセキュリティレビュー、後半11の未コミット変更による停止を確かめる。agent hookは実験的なため、失敗時は手動レビューへ切り替える。ローカルのスクリプトテストだけを、Claude CodeやWindows実機での完走と扱わない。
- **救済版は開催前に中身を見直す。** `main` 上の `starters/⟨題⟩/rescue/index.html` が実体で、**Gitタグは使わない**（タグ名と文書中のURLを人手で同期する方式は一度壊れた実績があるため）。開催前に各お題を実際に完成まで作り込んで差し替え、`.github/workflows/link-check.yml` が緑であることを確認する
- **拡張が起動しない系のトラブルは3段階で切り分ける**（00-prep.md 0.2に対処法を記載済みだが、提出スクショや当日の様子で見落としが疑われたら能動的に確認する）
  - **最初に見るのはVS Codeのバージョン（1.94.0以上か）**（[公式の拡張要件](https://code.claude.com/docs/en/vs-code#prerequisites)）。古ければ更新させる（00-prep.md 0.2の2）
  - Claude Code拡張を更新し、VS Codeを再起動させる（[公式のトラブル対処](https://code.claude.com/docs/en/vs-code#fix-common-issues)）
  - 解消しなければ、エラー文と画面を残してCLI版を試す。VS Codeのターミナルで、事前導入した `claude` を起動する。ログインを求められたら案内に従わせる。`claude` が見つからない場合は00-prep.md 0.2の5で導入とPATH設定を確認し、VS Codeを再起動して `claude --version` を再確認する。**講座の作業はCLI版でも進められる。** メンター側もこの順序と切り替え手順を把握しておく

## 前半の進行キュー

| セクション | 講師の要点 | メンターの動き |
|---|---|---|
| 1. オープニング | 今日の全体地図（前半→宿題→後半）を映しっぱなしにする。最後に**`/help` を投影で1回見せてから、全員で `/status` → `/model sonnet` → `/effort medium` → `/usage` を叩かせる**。前半・後半共通の推奨は **Sonnet × medium** と口頭でも伝える。**Maxプランの既定はOpusなので、Maxの人ほど `/model sonnet` を忘れやすい**と一言添える。バイブコーディングの説明では「コードが読める人も手で直さない。作るのはAI、品質を確かめるのは自分」を必ず言う | 巡回してモデルとeffortの設定で固まっている人を拾う。残量が少ない人を把握 |
| 2. ハンズオン① | **バイブコーディング→MCP→エージェントスキルの3本柱を説明し、バイブコーディングとエージェントスキルを主線で実践させる。** 完成度を求めない。「頼んだら動いた」の驚きを優先させる。**主線は「作る→見る→直す」→`tsumete` の簡易版→`/config`→`/clear`。** 各実践の開始時に「それでは実際にここまで作業を進めてください。」と声をかけ、解説中は手を止めさせる。**MCP/Gmailの実践は任意課題とし、主線を早く終えた人から案内する。** claude.aiでGmailコネクタを接続させ、下書きメールを1通作らせる。**接続後は`/clear`ではなくClaude Codeの再起動が必要**であることを口頭で明確に伝える。VS Code拡張での再起動はコマンドパレットの `Developer: Reload Window`。**講師のデモでもターミナルの `claude` に切り替えない**（参加者が拡張からCLIへ移ってしまう）。再起動後は `/mcp` でConnectedを確認するところまで投影する。個人のGmailを使うことに抵抗がある人は無理に接続させず、隣の画面を見学でも可。**Gmailを試さない人は主線をそのまま進める。** `tsumete` は一問一答の簡易版でよい。`.claude/skills/`（プロジェクトレベル）に保存させ `/tsumete` で動くところまで確認させる。**先に動いた人はまずGmail任意課題、次に🚀拡張ミッション①へ誘導し、ハンズオン③へ先回りさせない**（③は `myapp` で作るので、`vibecoding-0908` で先に育てても作り直しになる）。**`disable-model-invocation: true` は①から入れさせる（呼び出しは最初から最後まで `/tsumete` で統一）。**そのあと**全員に `/config` を開かせる**（余裕があれば `/context` も）。最後に `/clear` を必ず全員に。| 権限プロンプトで固まる人・`gh repo create` で認証エラーになる人を最優先で拾う。**claude.aiでコネクタは接続済みなのにClaude Codeから使えない人は、ほぼ全員「再起動していない」が原因。** `/clear`だけして再挑戦している参加者を見つけたら、Claude Codeそのものを終了→再度開かせ、`/mcp` でConnectedを確認させる。Google側のOAuth画面で止まる人（2要素認証・会社アカウントのポリシー制限）は任意課題なので主線を優先し、詰まったら接続を中断して主線か見学に切り替えさせる。「2番を読まずに連打」を見つけたら止める。**Auto Modeで確認自体が少ないぶん、逆に「何も起きていないように見えて不安」と申告する人が出やすい。画面のツール実行表示を一緒に見て、実際にファイルが動いていることを確認させる** |
| 3. ハンズオン② | `myapp` を開き直し、まず `CLAUDE.md` にルールを置く。壁打ちでは評価と実装を止め、一つずつ質問して広げる。メンターも結論へ誘導せず、本人が一案を選んだら4点に収束させる。そのあと中心操作だけを試作して触り、気づきをREADMEに戻し、デモだけを削除する。READMEをコミットし、プッシュは後半10まで待つ | アイデアが出ない人には最近の困りごとを聞く。試作が本実装へ膨らまないよう、中心操作一つに絞る。`tsumete` を `myapp/.claude/skills/` にコピーし、新しい置き場所を認識させるためClaude Codeを再起動する |
| 休憩 | 前半の折り返し地点。前半最長の枠（ハンズオン①）はもう終えている。次のハンズオン③に備えて一度離席させる | 巡回不要。この間に前半4提出物（チェックポイント3）の確認体制を確認しておく |
| 4. ハンズオン③ | **`tsumete`（①で作成済み）を、`tsumetsume`・`ddd` に役割を分けて育てさせる。** `tsumetsume`・`ddd` は新規作成、`tsumete` は更新。descriptionの書き方が肝であることを必ず口頭で。1つ目（tsumetsume）は自分の言葉で書かせ、ddd・tsumete更新は雛形寄りでよいと伝えて時間を守る。**`tsumete` の `disable-model-invocation: true`（①から入っている）の意味——中身を持つエージェントスキルは自動で呼ばれてよい／入口は人間が明示的に呼ぶ——を、3つに分けたこの場で必ず口頭で補強する。`tsumetsume`・`ddd` にはこの行を入れさせない** | `tsumetsume`・`ddd` が呼ばれない参加者の多くはdescription不足。一緒に読んで直させる。`tsumete` の参照名ズレ（保存した名前と本文中の名前が違う）にも注意。**すでに `myapp` の `.claude/skills/` にある `tsumete` を上書き更新している**ことを見失っている参加者がいたら、置き場所（②で学んだ内容）を思い出させる。VS Codeで開いているのが `vibecoding-0908` のままだと、3つが `vibecoding-0908` 側に保存されてしまうので、開いているフォルダも確認する。**「『詰めて』と書いたのに動かない」は仕様どおり。`/tsumete` に切り替えさせる（①から一貫してこの呼び方）** |
| 5. ハンズオン④ | Hooksのハンズオンは必修。編集ログに1行増えることを本人に確かめさせる。後半9では、この設定を残してコミット前レビューのフックを追加する。時間が余れば、ほかに保存したい作法を考えさせる | `/hooks` で登録を確認し、必要ならClaude Codeを開き直す。既存の `settings.json` を丸ごと上書きさせない |
| 6. まとめ・宿題説明 | 締切と提出先、後半で実装に使える時間を伝える。前半3で置いた `CLAUDE.md` を読み直し、ブラウザだけ、外部連携なし、時間内に作れる規模を再確認してから `/tsumete` を使わせる。READMEには望む体験と完成判定を残し、TASKSは状態／やること／完了定義の3列にする | 宿題の保存はローカルコミット。プッシュは後半10のレビュー後で、提出フォームへのREADME/TASKS提出は従来どおり。CONTEXTやADRは確定した用語や決定がある場合に残る |

## 宿題期間の運営

- 宿題提出は **`README.md` と `TASKS.md` の2ファイル**。後半開始の**前日中**を締切とする
- **チェックポイント3（3つの `SKILL.md` の中身。`myapp` の `.claude/skills/` にあるが、`myapp` は非公開リポジトリなので、フォームは貼り付け or アップロードで受け取る）は前半終了後〜当日夜に、担当メンター1名を決めて目を通す。**（前半の休憩はハンズオン③（提出物が出るタイミング）より前の1回のみ。チェックポイント3の提出後にまとまった休憩は無いので、実運用はこのタイミングになる）
  - 見るのは3点: **`tsumetsume`・`ddd` の** `description` に「いつ使うか」が入っているか（`tsumete` は `disable-model-invocation: true` なので呼び出し語は不要）／`tsumete` 本文が参照するエージェントスキル名が保存名と一致しているか／`tsumete` 末尾にREADME.md・TASKS.md書き出しの指示があるか
  - 3点目が無い人には宿題着手前に連絡するのが理想だが、6終了直後に着手する参加者もいて間に合わないことがある。**取りこぼしは後半8冒頭のTASKS.md生成プロンプト（02-part2 §7）で回収できるので必須ではなく「できれば前倒し」**
- メンターは提出物に目を通し、次のいずれかに該当するものをリストアップしておく
  - **`TASKS.md` が無い**: 自作 `tsumete` に書き出しの指示を入れ忘れている。後半8の冒頭で作らせれば追いつく（02-part2の§7表の下にあるプロンプトを使う）
  - **大きすぎる**: 後半の実装時間で終わらない規模。**ただし「やることが多い」こと自体は問題ではない**（後半8で今日やる分を決める）。問題なのは1つ1つが大きい場合
  - **曖昧さが残っている**: 「使いやすく」など目で確認できない表現がまだある。前日中に直せるなら直させる。直らないまま当日を迎えたら、後半7で見本（`starters/c-habit-tracker/homework/`、5ファイルの丸写し）へ誘導する
  - **制約違反**: ログイン・サーバー・外部サービス連携が「できること」に残っている。**後半8で発覚すると実装時間が崩れるので、前日中に必ず潰す。** 該当者には `myapp` に `CLAUDE.md` を置いたかも聞く（置いていれば、詰め直しのときに制約が毎回読まれる）
  - **未提出**: 後半7（宿題ふりかえり）で個別フォローが必要。見本の丸写し（本線はClaude Codeに頼む方法）で8に間に合わせる
- **宿題期間に相談の場を設ける場合は、アイデアの壁打ちと技術的な詰まりの相談を別の枠に分ける。** 日時と参加方法は前半の最後に口頭で伝え、あとからチャットにも流す。前半の録画を共有する場合も、ここで場所を案内する
- 該当者には後半開始前、または7の時間に声をかける。頭ごなしに直させず、「今日の実装時間で終わる形」に絞る手伝いをする

## 後半の進行キュー

| セクション | 講師の要点 | メンターの動き |
|---|---|---|
| 7. 宿題ふりかえり | **全員に `myapp` のフォルダを開かせるところから。** `/usage` と `/status` を確認させ、`/model sonnet` → `/effort medium` で推奨設定に戻す。手元にあるものを3段階で自己判定させる。**`CLAUDE.md` が無い人（宿題未着手・見本を使う人を含む）には、9の実装前に前半3のプロンプトで置かせる**（後半9の実装と後半10のレビューが3つの制約を読む前提になっている）。**「Cloudflareアカウントは9の生成待ち時間に作っておいて」を全体アナウンス** | 宿題リストの分類（TASKS.md欠落／大きすぎ／曖昧／制約違反／未提出）を個別に回る。**曖昧・未提出は見本の丸写しへ。** 自分で書いたものは `myidea/` に退避させ、上書きさせない（本文の3通りのうち、非エンジニアにはClaude Codeに頼む方法だけを案内する） |
| 8. ハンズオン⑤ 今日やる分を決める | **冒頭でまず「TASKS.mdが無い人は§7のプロンプトを貼って」と全体アナウンス。** そのあとステップ1（粒度を揃える）→ステップ2（MVPに絞り込む）の順。**「MVPは、それだけで動くものが1本通る組み合わせ」を必ず一度言わせる。** 上から何個、ではない。**上限は時間で決まる（実装時間÷15分）** | 絞れない参加者を手伝う。迷ったら減らす方へ倒させる |
| 9. ハンズオン⑥ 実装 | 最初に同梱の `/code-review` と、コミット前のフックを用意して動作確認する。1件の完了定義を読む→最小実装→本人がブラウザ確認→TASKS更新→レビュー→コミットを投影する。15分ごとに「動いたか、確認したか、結果を待ってレビューしたか、コミットしたか」を確認する。プッシュはしない | 古い教材の同名 `code-review` / `security-review` が読み込まれていないか確認する。制御ファイルはレビュー合格の証明ではない。agent hookが使えない場合も、毎コミット前に手動のセキュリティレビューを行う。TDDは希望者だけの任意課題 |
| 10. ハンズオン⑦ 通し確認とプッシュ | READMEの完成判定を一覧にし、本人が通しで操作する。修正したら以前の機能も再確認する。初回プッシュ前に `/security-review` で未送信コミット全体を確認し、CriticalとHighを解消してからプッシュする | コードの不具合、仕様への適合、セキュリティを混同させない。見送った指摘は「レビューで残した指摘」に理由つきで残し、セキュリティ指摘には重大度も付ける。GitHubの履歴に届いたことまで確認する |
| 11. ハンズオン⑧ デプロイ | 公開用の `public/` 分離を飛ばさせない。`wrangler whoami` のアカウント名と `assets.directory` を本人が確認してから公開を指示する。未コミット変更を止めるフックを追加し、設定と公開用ファイルもコミット前レビューに通す | `node -v` が通らなければ事前準備0.2へ。Cloudflareアカウント作成やログインは本文に従う。workers.devのサブドメイン登録エラーは、表示されたダッシュボードURLで本人が登録する。別端末か別ブラウザプロファイルで公開URLを確認する。localStorageの非共有と、ファイルへのデータ混入を分けて確かめる |
| 12. ハンズオン⑨ MCP | **NotionかBacklogのどちらか片方でよい**と最初に言う（迷ったらNotion）。**Claude Codeの再起動が必要**を全体アナウンス（Notionは `Developer: Reload Window`、Backlogは `.env` を読み込んだターミナルタブで `claude`）。**「TASKS.mdを置き換えるのではなく出口を1本つけるだけ」**を必ず言う | **Backlogを選んだ人は、スコープが `user` であること／登録コマンドの `${BACKLOG_API_KEY}` が実際の値に展開されず参照のままであること／`.env` が `.gitignore` に入っていることの3点を必ず目視確認する。`project` スコープや展開済みの値はAPIキーが公開リポジトリや会話履歴に乗る事故**。Node.jsが無い人はNotionへ。接続が通らない人は手順を見るだけで可 |
| 13. 成果共有 | 発表者は9の巡回中に目星を付けておく（エンジニア1・非エンジニア1・意外なアイデア1）。**公開URLがある人は画面に出させる** | 発表者の画面共有段取り |
| 14. クロージング | 一連の流れの図で締める。**つないだコネクタ（Gmail・Notion）の外し方が資料にあることを口頭で1回示す**（外すこと自体は推奨しない。場所を知らせるだけ）。**「エージェントスキルに数行足しただけで宿題の持ち物が変わった」**と**「工程を小さく分けて噛み合わせた」**の2つを最後に言う。Maxアップグレードの案内は「枠が足りなかった人向け」の実務トーンで | — |

## 時間が押したときの切り方（優先順）

1. 任意のGmail連携、TDD、発展的な試作は省く。前半5の編集ログのフックは必修とし、説明を短くする。
2. 前半4で参加者が大きく遅れている場合、`ddd` は「答え合わせ用の完成例を眺めるだけ」に切り替えさせ、`tsumete` は `tsumetsume` だけを参照する簡易版（1問1答のみ、書き残しは無し）で妥協させる。**tsumetsumeを自分の言葉で書く体験だけは死守する。** この場合、チェックポイント3は「`SKILL.md` 2つ＋`/tsumete` で質問が返る」までで可と全体アナウンスし、`CONTEXT.md`/ADRの確認要件は外す。ただし `tsumete` 末尾のREADME.md・TASKS.md書き出し指示だけは簡易版にも必ず入れさせる（宿題の持ち物に直結するため）
3. **後半12（MCP連携）を全カットして13へ飛ばす。** 後半でいちばん切りやすい。3つの見出しは `TASKS.md` に残っているので、持ち帰りは成立する
4. 後半13の発表を3名→2名に減らす
5. 前半2の「設定を開いて中を見る」を、`/config` だけに絞る（`/context` は飛ばし、`/clear` は必ずやらせる）
6. **切ってはいけないもの**（時間が無いときの調整はここまで。以下は削らない）
   - 後半9のコミット前レビューと、後半10の完成判定、初回プッシュ前のセキュリティレビュー。agent hookが使えない場合は手動レビューへ切り替える。
   - 後半11のステップ1（`public/` 分離）。**ここを飛ばすと仕様書・`.claude/` ごと公開される。デプロイ自体を切るのは可だが、分離を飛ばして公開させるのは不可**
   - 前半4のtsumetsume自作（アイデア言語化＋Grill Me系エージェントスキルの核）
   - 前半5のHooksハンズオン。**参加者が自分でHookを設定しログ1行を確認するところまでが必修＝チェックポイント4。** 時間が無いときは講師デモを先に見せて設定作業を短縮してよいが、**デモだけで終わらせない**
   - 後半9の15分チェックポイント運用
   - 後半8で今日やる分を決める（飛ばすと実装が終わらない）

## トラブル対応表

| 事象 | 対応 |
|---|---|
| 利用枠の上限到達（Pro） | メンター機（Max）でペア作業。本人に操作させ、メンターは枠を貸すだけ。5時間ウィンドウのローリング回復も案内 |
| Gmailコネクタの接続でOAuth画面が進まない・2FAで止まる | **任意課題なので主線を止めない。** 時間がかかりそうなら接続を中断して主線か拡張ミッションに戻し、希望者は隣の画面を見学する |
| 会社支給のGoogleアカウントで接続が拒否される（組織ポリシー） | 個人アカウントへの切り替えを勧める。個人アカウントも持っていない・作りたくない場合は接続せず見学に回す（本線への影響なし） |
| コネクタを接続したのにClaude Codeから使えない | **`/clear` では直らない。Claude Codeを再起動しているか確認。** VS Code拡張ならコマンドパレットの `Developer: Reload Window`、ターミナルなら `/exit` してから `claude`。MCPの接続はプロセス起動時に確立されるため、会話を切っただけでは反映されない。再起動後は `/mcp` でConnectedになっているか確認させる |
| 拡張が起動しない・反応しない | VS Codeが1.94.0以上か確認→Claude Code拡張を更新→VS Codeを再起動。直らなければエラー文と画面を残し、ターミナルで `claude` を起動する（未導入なら00-prep.md 0.2の5へ） |
| 拡張がTypeErrorなどでクラッシュする | エラー文と画面を残し、上の行と同じ順に確認する。直らなければVS Codeのターミナルで `claude` を起動する（見つからない場合は00-prep.md 0.2の5で導入とPATH設定を確認） |
| 全体的に応答が遅い（障害・混雑） | ハンズオンは待ち時間に言語化ワーク・仕様の見直しを前倒しする |
| 環境が完全に壊れた参加者 | まずファイルをコピーして退避する。GitHub経由で別環境へ移る場合は、未送信コミットのセキュリティレビューを済ませてからプッシュする。未プッシュなら、退避した仕様とエージェントスキルを引き継ぐ |
| 後半10でプッシュが弾かれる（コミットは通るが push だけ失敗する） | `gh auth status` を確認させる。**未認証・別アカウントでログインしている人**→ブラウザでGitHubログインを通し直させる。**そもそも `myapp` を作れていない人**→ハンズオン②の手順に戻ってその場で作り直す（`gh` コマンドがだめならWeb UI手動作成＋clone） |
| エージェントスキルが作ったはずなのに呼ばれない・pushしたファイルが想定と違う場所にある | **リポジトリ作成直後にVS Codeを開き直していない可能性が高い。** Claude Codeがまだ1つ前のフォルダを見ている状態で作業を続けている。`~/github/vibecoding-0908`（または `myapp`）を直接開き直させる |
| アイデア乗り換えを始めた参加者 | 「メモして持ち帰る」に誘導。乗り換えは迷子の最大要因という理屈まで伝える |
| 宿題が未着手のまま後半に来た参加者 | 前半2〜3で作ったものを種に、後半7の時間で急いで1周させる。それでも決まらなければ [starters/](../starters/) のお題を仕様として仮採用させる |
| やることが20個ある参加者 | **減らさせない。** 後半8でMVPに絞り込ませ、残りは「持ち帰る分」へ移させる。詰めた量は持ち帰る資産だと伝える |
| レビューの指摘にどう対応すればよいか分からない | MVPに関わる不具合は直す。簡素化などの提案は理由つきで持ち帰り可。CriticalとHighは直すか、その機能をMVPから外し、再レビューする |
| コードレビューの確認で止まったのに `/code-review` が始まらない | 本人が `/code-review` と打ち、結果を待つ。制御ファイルは開始時点で更新されるので、印があることだけでコミットしてよいと判断しない |
| 後半11でデプロイがフックに止められる | 未コミット変更が残っていないか確認する。公開用ファイルと設定の変更をレビューしてコミットしてから再実行する。gitignoreに入っているファイルまで安全だと保証するフックではない |
| 後半12でBacklog MCPが接続できない | まず `node -v` を確認（事前準備0.2で全OS必須にしたが、未実施の人が残る可能性はある）。次に**`.env` を読み込んだのと同じターミナルタブで `claude` を起動し直しているか**を確認する（別タブで起動すると環境変数が引き継がれず、設定ファイルに `${BACKLOG_API_KEY}` が未展開のまま残って接続できない）。それでも通らなければ**Notionへ切り替えさせる。** APIキーの再発行を試す前にここを確認する |
| 早く終わった参加者 | 該当ハンズオンの🚀拡張ミッションへ誘導（エンジニアは〔Eng〕系）。本線の先回りは止める。メンター補助役を頼むのも可 |

## 実測データの収集（次回改善用）

各回で記録する:

- チェックポイント通過率（0〜7の各段階）
- 宿題提出率と、提出物の分類（TASKS.md欠落／大きすぎ／曖昧さ残り／制約違反／未提出。「宿題期間の運営」の5分類に準拠）の内訳
- 上限到達者数とその時刻・プラン内訳
- 参加者の平均ターン数の聞き取り（任意アンケート）
- 詰まりポイントのカテゴリ集計（環境／git・保存（gh認証/repo作成/push）／指示／エージェントスキル作成／宿題）
- 前半1・2・4の実所要時間（ハンズオン①へのtsumete作成追加＋Grill Me系の作法を3つに分ける構成で、どこに時間がかかったか）
- 任意課題のMCP/Gmailハンズオンの実所要時間と、コネクタ接続でつまずいた人数・原因（OAuth・2FA・組織ポリシー・個人アカウント忌避で見学に回った人数）
- 宿題提出物のうち「制約違反」（ログイン・サーバー必須）の件数

> ワークショップは実測で改訂する。アンケートに「宿題の負担感」を1問入れる（前半→後半の構成自体が今回の変更点のため）。
