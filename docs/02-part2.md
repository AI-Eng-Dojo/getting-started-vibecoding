# 後半：自分で考えたアプリを、ちゃんと動く状態で公開する

後半のゴール: **宿題で詰めてきた仕様とやることリストの中から「今日やる分」を決めて、上から順に実装、レビューを通し、インフラ構築して公開できる状態する**ところまでやります。

## やること

| 時間（案） | やること | この枠の成果物 |
|---|---|---|
| 0:00–0:05 | **7. 宿題ふりかえり** — 手元にあるものを3段階で確認する | — |
| 0:05–0:20 | **8. ハンズオン⑤ 今日やる分に線を引く** — `TASKS.md` の粒度を揃え、今日やる4枚を選ぶ | 線を引いた `TASKS.md` |
| 0:20–1:30 | **9. ハンズオン⑥ 実装**（メイン） — `TASKS.md` を司令塔に、`tdd` スキルで1件ずつ | 動くアプリ本体 |
| 1:30–2:07 | **10. ハンズオン⑦ レビューを仕組みにする** — 2軸レビュー＋セキュリティレビュー＋Hooksで「必ず通る」状態にする | レビュー結果・修正・Hook 2件 |
| 2:07–2:27 | **11. ハンズオン⑧ Cloudflareに公開する** — 自分のPCの中から、URLで見られる場所へ | 公開URL |
| 2:27–2:42 | **12. ハンズオン⑨ 仕事の道具とMCPでつなぐ** — 「今回やらないこと」をNotion／Backlogへ引き渡す | 明日から使えるタスク一覧 |
| 2:42–2:49 | **13. 成果共有** — 何を作ったか／どう相談したか／どこでAIが失敗したか | — |
| 2:49–2:55 | **14. クロージング** — つないだものの外し方・持ち帰るもの | — |

> **今日のメインは `TASKS.md` からMVPを実装する部分です。** くわえて11でCloudflare、12でNotionやBacklogといった外部ツールを利用しますが、**開発の計画は最初から最後まで `TASKS.md` でコントロールします。** 最低でもMVPの実装完了、できればCloudflareにデプロイするところまで進めましょう。

---

## 7. 宿題ふりかえり（5分）

まず、**宿題で使った `myapp` のフォルダ**をVS Codeで開いてください。メニューの「ファイル」→「フォルダーを開く…」から、**myappフォルダ**を選びます（`~/github/myapp`。普段の置き場所を使ってる人はそちらを指定）。

```
~/github/myapp/   ← これを開く
├── README.md      宿題で詰めた仕様
├── TASKS.md       宿題で作ったやることリスト
├── CONTEXT.md     用語集（あれば）
└── docs/adr/      決定記録（あれば）
```

次に `/usage` で利用枠の残量を、`/status` でいま選んでいるモデルを確認します。**残量が半分を切っている人はプランのアップグレードか軽量なモデルを利用してください。**

次に宿題がどこまでできているか確認してください。

| 手元にあるもの | このあとどうするか |
|---|---|
| **`README.md` と `TASKS.md` の両方が揃っている** | このまま8へ |
| **`README.md` はあるが `TASKS.md` が無い** | 8の冒頭で `TASKS.md` を作るところから始める（下のプロンプト） |
| **どちらも無い（宿題に着手できなかった）** | `vibecoding-0908` を使ってハンズオンを進めてください |

`TASKS.md` がまだ無い人は、8の冒頭でこれを貼ってください。

```text
README.md を読んで、実装する順に「やること」の一覧を TASKS.md に書き出してください。
1つは15分以内で終わる大きさにし、それぞれ「画面のどこを見れば終わったと分かるか」を書いてください。
上から順にやれば、途中で止まっても動くものが残る順番にしてください。
```

### 今日のうちに済ませておく1つ（11の準備）

後半11で、作ったものを**Cloudflareに公開**します。**アカウントが無くても試せるルートを用意してある**ので今すぐ登録する必要はありませんが、公開したものを1時間以上残したい人は無料アカウントが要ります。**9の実装中、Claudeがコードを書いている待ち時間**に [dash.cloudflare.com](https://dash.cloudflare.com) を開き、**「Sign in with Google」から個人のGoogleアカウント**で登録しておくと、11がそのまま流れます（事前準備0.1④と同じ経路）。

## 8. ハンズオン⑤ 今日やる分に線を引く（15分）

宿題で詰めた人ほど、`TASKS.md` のやることは多くなっているはずです。**それは正しい状態です。** 減らす必要はありません。ここでやるのは、**今日触る分と、持ち帰る分に線を引くこと**だけです。

### なぜ線を引くのか

実装は70分です。やることが10個あれば1つ7分。**丁寧に詰めた人ほど1つあたりの時間が短くなって終わらない**——これを防ぎます。

### ステップ1: 粒度を揃える（5分）

まず、大きすぎるものを割ります。

```text
TASKS.md を読んで、次を指摘してください。
- 15分では終わらなさそうな大きさのもの
- 「画面のどこを見れば終わったと分かるか」が書かれていないもの
- 順番を入れ替えたほうが、途中で止まっても動くものが残るもの

指摘だけしてください。まだ直さないでください。
```

指摘を**自分の言葉で読み**、納得したものだけ反映させます。全部を鵜呑みにする必要はありません。

```text
1と3を反映してください。2は今日は対象外にします。
```

### ステップ2: 今日やる4つを選ぶ（10分）

ここが後半の勝負どころです。

```text
TASKS.md のやることを、今日やる4つと、持ち帰る分に分けてください。

今日の4つは、それだけで「動くものが1本通る」組み合わせにしてください。
残りは TASKS.md の「今回やらないこと」に移し、なぜ今日やらないかを一行ずつ書いてください。
```

**「上から4つ」ではなく「動くものが1本通る4つ」**が肝心です。入力・処理・表示が揃わないと画面が動きません。印刷や見た目の仕上げは自然に後ろへ落ちます。

出てきた4つを見て、**自分で納得できなければ入れ替えてください。** 決めるのはあなたです。

> **4つが多い・少ないと感じたら調整して構いません。** 迷ったら**減らす**ほうに倒してください。3つ動いて終わるほうが、5つ中途半端より確実に良い一日になります。

> **「今回やらないこと」は捨て場所ではありません。** ここに書いたものが、12でそのままNotionのページやBacklogの課題になり、明日からの続きになります。**理由を一行書いておくと、明日の自分が判断をやり直さずに済みます。**

### ✅ チェックポイント5

- [ ] `TASKS.md` の今日やる分が4つ以内に絞られている
- [ ] それぞれに「画面のどこを見れば終わったと分かるか」が書いてある
- [ ] やらないと決めたものが、理由つきで残っている
- [ ] **最初の1つが何かがはっきりしている**

## 9. ハンズオン⑥ 実装（70分・メイン）

ここが後半の中心です。**全員同じアプリを作るのではなく、同じプロセスを使ってそれぞれ違うものを作ります。**

> **この70分、あなたはファイルを一度も自分の手で編集しません。** コードも `TASKS.md` も、書き換えるのはすべてClaude Codeです。あなたの仕事は**指示を出す・画面を見る・目印で判定する**の3つだけ。手で直したくなったら、それは指示で伝えるべきことです。

### まず、実装の進め方をスキルに任せる（5分）

前半4では「詰めるスキル」を自分で作りました。**後半で使うのは、実装の進め方そのものをスキルにしたもの**です。完成例が [skills/tdd/SKILL.md](../skills/tdd/SKILL.md) にあります。前半のGrill Me系Skillと違い、**これは自分で書かず、コピーして使って構いません。**

```text
次のURLの中身を、~/.claude/skills/tdd/SKILL.md として保存してください。
フォルダが無ければ作ってください。

https://raw.githubusercontent.com/AI-Eng-Dojo/getting-started-vibecoding/main/skills/tdd/SKILL.md
```

> **教材リポジトリはcloneしていないので、URLで渡します。** 取得の許可を聞かれたら許可してください。
> 取得できなかった場合は、上のURLをブラウザで開いて全文をコピーし、Claude Codeに貼って「これを `~/.claude/skills/tdd/SKILL.md` として保存して」と頼めば同じことができます。

保存できたら `/clear` で会話を切ってください。**スキルは新しい会話から読み込まれます。**

> **なぜスキルにするのか。** この70分で、あなたは同じ4ステップを4〜5周します。**毎回同じことを言う代わりに、一度ファイルに書いておく**——前半4でやったことと、まったく同じ理由です。今日はそれを、対話の作法ではなく**開発工程そのもの**に対してやっています。

### この70分の回し方

**`TASKS.md` を中心に置いた4ステップを、15分ごとに1周させます。** 何をやっているか分からなくなったら、いつでもこの図に戻ってください。

```
   ┌──────────────────────────────────────────┐
   │  TASKS.md（司令塔・常に最新）              │
   └──────────────────────────────────────────┘
        ↓ ①上から1つ取る          ↑ ④終わった分を反映させる
   ┌─────────────┐          ┌─────────────┐
   │ ②最小の形で実装させる │ ──────→ │ ③目印を自分の目で確認 │
   └─────────────┘          └─────────────┘
                                      ↓ OKなら コミット＋プッシュ
```

回し始めるのに必要なのは、この一言だけです。

```text
TASKS.md の次のやることを実装してください。
```

`tdd` スキルが起動すると、**いきなりコードを書き始めず、まず「終わったと分かる目印」を読み上げて確認**してきます。**そこが出発点です。** 目印が書かれていないやることは、ここで目印を決めるところから始まります。

#### このスキルが守っている3つ

| ルール | なぜ |
|---|---|
| **1回に1件だけ** | まとめて実装すると、動かなかったときにどれが原因か分からない |
| **目印は「画面で見えること」だけ** | 「関数が正しく実装されている」は目印になりません。**あなたが操作して確認できること**だけです |
| **「終わった」と決めるのは、あなた** | Claudeが自分の実装を自分で確かめても、何も確かめたことになりません |

3つ目がいちばん大事です。**Claudeが「できました」と言っても、それは終わったという意味ではありません。** あなたがブラウザで目印を見て、初めて終わります。スキルはそこで**確認を求めて止まる**ように書いてあります。

#### 15分ごとのチェックポイント運用

講師の合図（または自分のタイマー）で、次を実行してください。

まず、**`TASKS.md` に書いた「終わったと分かる目印」を、自分の目で確認します。**

| 目印の確認 | どうするか |
|---|---|
| **OK** | **コミットしてプッシュする**（下のプロンプト） |
| **NG** | 前半で覚えた**リカバリー3手**（貼る・戻す・小さくする）で動く状態に戻してから、あらためて確認 |

目印がOKなら、そのまま頼んでください。**gitが分からなくても構いません。**

```text
目印を確認できました。いまの状態をコミットして、プッシュしてください。
```

**確認できたものだけを残していく**——これが今日の保存の作法です。動いていないものはコミットしません。

> プッシュ先は**ハンズオン②で作った `myapp`（自分だけのリポジトリ）**です。ほかの参加者と衝突することはないので、遠慮なく何度でもプッシュしてください。

#### スキルが呼ばれないときは

前半3で体験したとおり、**スキルを使うかどうかは最終的にClaudeが判断します。** 目印の確認を飛ばしていきなりコードを書き始めたら、4ステップを手で指示してください。**結果は同じです**（コピペ用テンプレ: [templates/first-instruction.md](../templates/first-instruction.md)）。

```text
TASKS.md の1つ目だけを、最小の形で実装してください。
他のものにはまだ手をつけないでください。
```

```text
いま終わった分を、TASKS.md の状態に反映してください。
```

**どこまで来たかが常にファイルに残っている**のが、この70分の正しい進み方です。

> **この70分の間は、セキュリティレビューのHookはまだ入れません。** 15分ごとのプッシュが止まってしまうからです。**先に作りきり、10でレビューを仕組みにしてから、11で外に出す**——この順番自体が今日の設計です。

> **`tdd` はリファクタリングをしません。** 読みにくいコードや重複は、この70分では放置して構いません。**整えるのは動いたあと**——10の `code-review` の仕事です。工程を分けているのは、**同時にやると片方が雑になる**からです。

### 詰まったときのエスカレーション順

1. リカバリー3手（貼る・戻す・小さくする）— まず自分で2回試す
2. `/model` で重いモデルに切り替えて、同じことをもう一度頼む
3. 挙手 → メンターが巡回します
4. それでも詰まったら、やることを削ってでも「動く状態」を優先する。**完成度より、確認できる状態を保つことが優先です**

### よくある状況と対処

| 状況 | 対処 |
|---|---|
| 最初の1つが15分経っても動かない | 大きすぎるサイン。メンターと一緒にさらに小さく割る |
| 予定より早く4つとも終わった | 「今回やらないこと」から1つ戻して、同じ型でもう一周する |
| 途中でもっと良いアイデアを思いついた | **今日は乗り換えない。** 「思いついたことを TASKS.md の『今回やらないこと』に書き足して」と頼んで持ち帰る |
| 利用枠の上限に当たった | 挙手 → メンター機でペア作業に切り替え |
| `CONTEXT.md` と違う言葉で実装されている | 「`CONTEXT.md` の用語に合わせて」と伝える。宿題で用語集を作った人はここで効きます |
| どの案でいくか決められなくて止まっている | **本実装を続けない。** `prototype` スキル（拡張ミッション③）で、捨てる前提の小さいものを作って決める |
| どうしても `myapp` が動く形にならない | **リカバリー3手で、最後に動いていた状態まで戻してください。** 今日やる4つのうち、動いたところまでで構いません。**動かないものは直すのではなく削ります。** 10以降は「動いているものをレビューして、公開して、引き渡す」工程なので、**動く画面が1つあれば全部通ります。** 以降もVS Codeで開くのは `myapp` のままです |

### ✅ チェックポイント6

今日やると決めた4つのうち、**主要なものが動いている**状態で10へ。全部揃っていなくても構いません。仕上げの時間で追い込みます。

### 🚀 拡張ミッション③（早く終わった人向け・任意）

- 〔共通〕**「今回やらないこと」から1つ戻す**: 今日の型（最小実装 → 目印で確認 → 保存）でもう一周する
- 〔Eng〕**ブランチを切ってPRを出し、レビューをもらう**: 本線では main にそのままコミットしていますが、実務ではブランチを切り、プルリクエスト（PR）にして他の人の目を通します。ここもClaude Codeに頼めます

    ```text
    このやることの分をブランチに切ってコミットし、プッシュして、プルリクエストを作ってください。
    PRの説明には、何を変えたかと、どこを見れば動作を確認できるかを書いてください。
    ```

    PRの向き先は**自分の `myapp` の中**（`main` ← 作業ブランチ）です。**`myapp` は非公開なので、URLを渡しただけでは相手から見えません。** 隣の人に見てもらうには、次のどちらかを先にやります。

    | やり方 | どうするか | 向いている場面 |
    |---|---|---|
    | **相手を1人だけ招く**（推奨） | GitHubの `myapp` → Settings → Collaborators → 隣の人のアカウントを追加 | **中身を不特定多数に晒さずに済みます** |
    | 公開に切り替える | Settings → General → 一番下の Danger Zone → Change visibility → Public | 中身を見られて困らないと自分で判断できるとき |

    PRができたらURLを隣の人に渡し、**レビューしてもらってください。** 見る側は「どこを見れば動くと分かるか」がPRに書かれているかを確認します。**「目印を書く」という今日の作法が、そのままレビュー可能性になる**——これが実務との接続点です
- 〔Eng〕**完了条件を渡して任せてみる**: `TASKS.md` の1つを、その「終わったと分かる目印」ごと `/goal` に渡し、満たすまで自走させてみる。**これが「任せきる」の入口です**（→ [columns.md](columns.md) ★7）
- 〔共通〕**`prototype` で迷いを潰す**: 決められない設計や「この動きで合っているか分からない」を、**捨てる前提の小さい試作**で判断します。完成例が [skills/prototype/SKILL.md](../skills/prototype/SKILL.md) にあります

    ```text
    次のURLの中身を、~/.claude/skills/prototype/SKILL.md として保存してください。

    https://raw.githubusercontent.com/AI-Eng-Dojo/getting-started-vibecoding/main/skills/prototype/SKILL.md
    ```

    保存して `/clear` したら、迷っているところをそのまま伝えてください（「一覧の並び順を新しい順にするか、期限が近い順にするか決められない」など）。**両方を1画面で切り替えられる試作**が出てきます。決まったら試作は捨て、決めたことだけを `CONTEXT.md` に残します。**本実装させる前に、判断材料を先に作る**——これが今日の型のもう一段上です
- 〔Eng〕**11の前に、自分で構成を設計してみる**: 「このアプリをどこで動かし、データはどこに置くべきか」をClaudeに設計させ、選択理由を説明させてください。**11でCloudflareに出す手順と答え合わせできます。** なぜ静的ホスティングで足りるのか、どこから先はサーバーが要るのかを、自分の言葉で言えるようになるのが狙いです

## 10. ハンズオン⑦ レビューを仕組みにする（37分）

動いたら終わりではありません。**この直後、11で本当に外へ公開します。** 公開してしまってからでは取り返しがつかないものがあるので、**出す前に必ずレビューが通っている状態**をここで作ります。

前半5で触れた「Skillは判断を介する、Hooksは条件が合えば確実に動く」を、ここで実戦投入します。

### 「レビューして」は、1つの質問ではありません

一言で「レビューして」と頼むと、返ってくる指摘は全部混ざります。**混ざったリストは、どれが重いのか分からなくなります。** ここでは**3つの別々の質問**として扱います。

| 質問 | 担当 |
|---|---|
| **頼んだものを作ったか**（仕様どおりか） | `code-review` の Spec 軸 |
| **まともな書き方をしているか**（作法どおりか） | `code-review` の Standards 軸 |
| **危ないところは無いか** | `security-review` |

上2つを分ける理由は、**片方が片方を隠すから**です。

- **作法は完璧だが、違うものを作っている** → 作法だけ見ていたら合格に見える
- **頼まれたとおりだが、書き方が散らかっている** → 仕様だけ見ていたら合格に見える

この2つはまったく別の状態です。1本のリストにまとめると、見えなくなります。

### ① レビュースキルを2枚用意する（6分）

完成例が [skills/code-review/SKILL.md](../skills/code-review/SKILL.md) と [skills/security-review/SKILL.md](../skills/security-review/SKILL.md) にあります。**どちらも自分で書かず、コピーして使って構いません。**

```text
次の2つのURLの中身を、それぞれ
~/.claude/skills/code-review/SKILL.md と
~/.claude/skills/security-review/SKILL.md として保存してください。
フォルダが無ければ作ってください。

https://raw.githubusercontent.com/AI-Eng-Dojo/getting-started-vibecoding/main/skills/code-review/SKILL.md
https://raw.githubusercontent.com/AI-Eng-Dojo/getting-started-vibecoding/main/skills/security-review/SKILL.md
```

保存できたら `/clear` で会話を切ってください。

`security-review` の末尾には、**「レビューが終わったら `.claude/security-reviewed` という空ファイルを作る」**という一行が入っています。**「外に出してよい」という判断を、Claudeの記憶ではなくファイルとして残す**ための印で、これが④のHookの土台になります。**一度自分の目で見ておいてください。**

> **印が作られるのは、未解決の「重大度・高」が0件のときだけです。** 高が残っている状態では印が作られず、④のHookが公開を止めます。**「レビューを実施した」と「外に出してよい」は別物**で、印は後者を表します。
>
> **どうしても直しきれない高が残ったときは、** 「承知のうえで進めたい」と明示すれば印を作れます。そのとき何を未解決のまま出すのかが読み上げられ、`TASKS.md` に残ります。**黙って通り抜けることはできません。**

> **印を作るのは `security-review` だけです。** `code-review` はこの印に触りません。**外に出してよい条件は「危なくないこと」であって、「作法が綺麗なこと」ではない**——この線引きを、スキルの分担そのもので表しています。

### ② 2軸でレビューして、直す（12分）

```text
コードレビューして
```

**仕様どおりか**と**作法どおりか**が、**別々の見出しで並んで**返ってくるはずです。1本のリストになっていたら、「2つの軸に分けて」と言い直してください。

> **Spec軸が読むのは、宿題であなたが書いた `README.md` です。** ここが今日の隠れた山場かもしれません。**自分で書いた仕様が、人に読まれるだけの文章ではなく、機械がレビューに使う入力として実際に働きます。** 「使いやすく」「いい感じに」を残さずに書いた宿題が、ここで効きます。逆に曖昧なままだと、この軸は何も言えません。

出てきた指摘を**自分の言葉で読み**、直すものを選んでください。**全部直す必要はありません。**

> **作法（Standards）の指摘が読めなくても、大丈夫です。** ここはコードの書き方の話なので、**読めないのが普通**です。コードを読まずに判断する目安を3つ挙げます。
>
> | 見え方 | どうするか |
> |---|---|
> | **同じ種類の指摘が3つ以上並んでいる** | 直す価値があります。**同じ問題が繰り返し起きている**というサインです |
> | 1つだけ、ぽつんと出ている | 今日は対象外で構いません |
> | 読んでも意味が分からない | 「**この指摘を、コードを知らない人向けに言い換えて**」と聞いてください。それでも腑に落ちなければ**対象外**にします |
>
> **分からないものを直させるのが、いちばん危ない選択です。** この直後の11で公開するので、**動いているものを壊すリスクを取る場面ではありません。**
>
> **作法の指摘を全部「対象外」にしても、今日の完成判定には影響しません。** この12分で持ち帰ってほしいのは、指摘を直した数ではなく、**仕様の話と作法の話が別々に並んで出てきた**という事実のほうです。

```text
仕様の1と2、作法の1を直してください。残りは今日は対象外にします。
```

> **「今回やらないこと」に書いたものが実装されていたら、それは重い指摘です。** 動くから良い、ではありません。**決めたことと違うものができている**というサインなので、直すか、決定のほうを更新するか、どちらかを選んでください。

直さなかったものは、持ち帰りに回します。

```text
直さなかった指摘を、TASKS.md の「今回やらないこと」に書き足してください。
```

**司令塔は最後まで `TASKS.md` です。** レビューで出た宿題も、ここに集約させます。

### ③ セキュリティレビューを通す（4分）

**必ずこの順番です。** ②で直したことが、危なさの状態を変えている可能性があるからです。

```text
セキュリティレビューして
```

秘密情報の直書き・入力の未検証な使用などを、重大度つきで報告してくるはずです。見つかった問題は指示して直させてください（コピペ用テンプレ: [templates/verify-prompt.md](../templates/verify-prompt.md)。Skillが呼ばれないときはこちらで代用しても構いません）。

**直したあとは、もう一度セキュリティレビューを通してください。** 直したことで、危なさの状態がまた変わっているからです。**この「直したら通し直す」を、次の④で仕組みにします。**

> **「できました」を鵜呑みにしない。** Claudeは実際には確認せず「できています」と答えてしまうことがあります。最後は必ず**あなたの目**で動作を確認してください。あなたの確認が最終権限です。

### ④ Hookで「必ず実行される」に変える（15分）

ここまでは、**あなたが呼んだからレビューが走りました。** 呼ぶのを忘れたら実行されません。そして忘れやすいのは、まさに急いでいるとき——**公開する直前**です。

そこで、Hookを2つ設定します。**「レビュー済みの印」を軸にした、たった2行の仕組み**です。

| Hook | タイミング | やること | 意味 |
|---|---|---|---|
| **A** | コードを編集した直後（PostToolUse / Edit・Write） | レビュー済みの印を消す | 変更したらレビューはやり直し |
| **B** | 外に出す直前（PreToolUse / Bash で `git push` や `wrangler deploy`） | 印が無ければ止める | 未レビューのものは外に出せない |

**AとBが揃って、はじめて仕組みとして働きます。** 片方だけでは、一度レビューを通した後にいくら書き換えても素通りしてしまいます。

> **ただし、これは「保証」ではありません。** 捕捉できるのは、**Claude Codeが Edit・Write でファイルを変えたとき**と、**`git push`・`wrangler deploy` というコマンドを実行しようとしたとき**だけです。
>
> | 抜ける経路 | なぜ |
> |---|---|
> | 自分の手でファイルを直接編集する | Claude Codeを通らないのでHook Aが気づかない |
> | ブラウザから公開する（**ルートA**） | 同じく、Claude Codeを通らない |
> | 別の書き方で外に出す（`npm run deploy` など） | Hook Bはコマンドの文字列を見ているだけ |
>
> **今日の目的は、抜け道を塞ぎきることではありません。** 「Skillは呼べば動く、Hookは呼ばなくても止めてくれる」——この違いを、**止まる瞬間として自分の目で見る**のがこの15分の狙いです。仕事で本気で止めたいときは、**人の操作を待つHookではなく、CIやブランチ保護のように「その経路しか通れない」形**にします。

```text
このプロジェクトの .claude/settings.json に、次の2つのHookを設定してください。

【Hook A】
- タイミング: ファイルを編集・作成した直後（PostToolUse、対象はEditとWrite）
- 内容: .claude/security-reviewed というファイルがあれば削除する

【Hook B】
- タイミング: Bashコマンドの実行直前（PreToolUse、対象はBash）
- 内容: 実行されようとしているコマンドに "git push" または "wrangler deploy" が
  含まれていて、かつ .claude/security-reviewed が無い場合は、そのコマンドを止めて
  「先に『セキュリティレビューして』を実行してください」という理由を返す
- 判定の中身は、次のURLから .claude/hooks/require-security-review.sh として
  保存し、実行できるようにしてください。settings.json からはそれを呼ぶ形にします
  https://raw.githubusercontent.com/AI-Eng-Dojo/getting-started-vibecoding/main/hooks/require-security-review.sh

あわせて .gitignore に .claude/security-reviewed を追加してください。
settings.json や必要なフォルダが無ければ作成してください。
```

> **判定スクリプトは、自分で書かずコピーして使います。** `tdd` や `security-review` と同じ扱いです（→ [hooks/README.md](../hooks/README.md)）。
>
> **Hooksは権限プロンプトを経由せず自動実行されます**（前半5）。だから原則は「**中身を理解できるものだけを登録する**」です。**その場でAIに書かせたスクリプトを、書いた本人の説明だけを頼りに登録するのは、この原則を満たしていません。** 検証済みのものを配るのはそのためです。
>
> 40行ほどで日本語のコメントが付いています。**読める人は読んでください。読めなくても、動きはこのあと自分の目で確認します。**

#### 止まることを自分の目で確認する

設定できたら `/clear` で会話を切り、**わざと未レビューの状態を作って試します。**

1. アプリのファイルを何か1つ編集させる（色を変える、など何でもよい）→ **Hook Aが印を消します**
2. そのままプッシュを頼む → **Hook Bが止めるはずです**

```text
いまの状態をコミットして、プッシュしてください。
```

3. 止められたら、あらためて `セキュリティレビューして` を実行する → 印が作られます
4. もう一度プッシュを頼む → **今度は通ります**

**Skillは「呼べば動く」、Hookは「呼ばなくても止めてくれる」**——この違いを、止まる瞬間として自分の目で確認してください。ここが前半5の回収点です。

> **止まらないときは** `/hooks` で2つとも登録されているか確認してください。入っているのに動かない場合は、Claude Codeを一度終了して開き直してから再確認します。**それでも直らなければ、Hookは一旦諦めて①〜③のレビューだけで11へ進んでください。** 今日の完成判定には影響しません。

### ✅ チェックポイント7

- [ ] 2軸レビューを通し、**仕様の指摘と作法の指摘が別々に**並んで出てくることを見た
- [ ] セキュリティレビューを通し、**未解決の「重大度・高」が0件**になっている（中・低は残っていてよい）
- [ ] 直しきれない高が残った場合は、**承知のうえで進めると明示し、その内容が `TASKS.md` に残っている**
- [ ] 直さなかった指摘が `TASKS.md` に残っている
- [ ] （任意）Hookがプッシュを止めるところを自分の目で見た

## 11. ハンズオン⑧ Cloudflareに公開する（20分）

ここまで作ったものは、**あなたのPCの中にしかありません。** ファイルを開けば動きますが、隣の人に見せるには画面を覗いてもらうしかない。ここでそれを、**URLを送れば誰でも見られる状態**にします。

### なぜCloudflareなのか

今日のアプリは**ブラウザだけで動く静的なもの**（HTML・CSS・JavaScript）です。サーバー側で動く処理もデータベースもありません。この形は**静的ホスティング**という一番シンプルな置き方で公開でき、Cloudflareはそれを**無料枠で・世界中に配信される形で**提供しています。

> **Cloudflareの中でも今の本流は「Workers」です。** 以前は静的サイト向けに「Pages」という別サービスがありましたが、Cloudflareは現在**Workers側に機能投資を集約し、新規は Workers から始めることを推奨**しています（Pagesも引き続き利用可能です）。この教材もWorkers側の手順で書いています。

### ステップ1: 公開するフォルダを分ける（全員・5分）

**いま `myapp` フォルダには、公開したくないものが混ざっています。** `README.md`（仕様）、`TASKS.md`（やること）、`CONTEXT.md`、`.claude/`（設定）——これらは**あなたの作業用**であって、世界中に見せるものではありません。

フォルダごと投げると、**それら全部が一緒に公開されます。**

```text
公開用に public/ フォルダを作り、公開してよいファイルだけをそこに移動してください。
- 移動するもの: アプリのHTML・CSS・JavaScript・画像
- 移動しないもの: README.md・TASKS.md・CONTEXT.md・docs/・.claude/・.git/
移動したあと、ブラウザで public/index.html を開いて今までどおり動くことを確認できる状態にしてください。
```

移動できたら、**必ず一度 `public/index.html` をブラウザで開いて動作を確認してください。** ここで壊れていることに気づかずに公開すると、原因の切り分けが難しくなります。

そのうえで、もう一度だけレビューを通します。

```text
セキュリティレビューして。特に public/ に入れたファイルを、
これから全世界に公開してよいかという目で見てください。
```

**「他人に渡して困るものが残っていないか」——これはSkillの最後の観点そのものです。** 実装中は抽象的だったこの問いが、公開する直前にだけ具体的になります。

### ステップ2: 公開する（10分）

**ルートは2つあります。** 自分に合うほうを選んでください。**どちらでも「公開された」という体験は同じです。**

| | ルートA: ドラッグ&ドロップ | ルートB: コマンドで公開 |
|---|---|---|
| 誰向け | **全員（本線）** | 〔Eng〕コマンドに抵抗がない人 |
| 必要なもの | ブラウザだけ | Cloudflareアカウント（Node.jsは全員導入済み） |
| 所要 | 2分 | 5〜10分 |
| Hookは効くか | 効かない（Claude Codeを通らないため） | **効く**（`wrangler deploy` で止まる） |

#### ルートA: Cloudflare Drop にドラッグ&ドロップする（全員・本線）

1. ブラウザで [cloudflare.com/drop](https://cloudflare.com/drop) を開く
2. **ステップ1で作った `public/` フォルダ**を、そのままドラッグ&ドロップする（zipでも可）
3. URLが発行され、その場で公開されます

**アカウントは要りません。** ただし**このURLは1時間で消えます。** 残したい人は、表示されている **Claim** を押し、**「Sign in with Google」から個人のGoogleアカウント**でサインインしてください（事前準備0.1④と同じ経路です。アカウントが無ければその場で作られます）。Claimすると、そのままあなたのアカウントの中に恒久的なサイトとして残ります。

> **1時間で消えることは、今日はむしろ好都合です。** 「とりあえず人に見せる」だけなら消えたほうが後腐れがありません。**続きを作る気がある人だけClaimしてください。**

#### ルートB: `wrangler` で自分のアカウントに公開する〔Eng〕

Claude Codeにそのまま頼めます。

```text
このアプリを Cloudflare Workers の静的アセットとして公開したいです。
1. wrangler.jsonc を作り、assets の directory を ./public/ に向けてください（Worker のスクリプトは不要です）
2. npx wrangler login でログインしてください（ブラウザが開くので私が許可します）
3. npx wrangler whoami を実行して、ログインできているか確認してください。
   「You are not authenticated」と出たら、そのまま先に進まずに教えてください
4. 確認できたら npx wrangler deploy で公開し、発行されたURLを教えてください
```

> **3を飛ばさないでください。** `npx wrangler login` は、**ブラウザの許可画面まで進みきらずに終わってもターミナル側にエラーを出しません。** 「ブラウザが開いた」はログインできた印ではありません。`npx wrangler whoami` でアカウント名が表示されて初めて、ログインできています。ここを飛ばすと、失敗するのは4のデプロイなので、**「デプロイが失敗した」ように見えて原因が分かりません。**

**ここでHook Bに止められるはずです。** 1つ目で `wrangler.jsonc` を作った時点で**ファイルの書き込みが起きるので、Hook Aがレビュー済みの印を消します。** その状態で3つ目のデプロイに進むため、Hook Bが「印が無い」と判定して止める——という流れです。

**これは故障ではありません。** 止まったら、そのまま続けてください。

```text
セキュリティレビューして
```

レビューが通れば印が作り直されるので、もう一度デプロイを頼めば今度は通ります。**「設定を1行でも足したら、外に出す前にもう一度見る」が仕組みとして効いた瞬間**です。面倒に見えますが、止めてほしいのはまさにこういう「あと一歩」の場面です。

できあがる `wrangler.jsonc` はこれだけです。**中身を見て、何を公開しているのかを自分で把握してください。**

```jsonc
{
  "name": "myapp",
  "compatibility_date": "2026-08-29",
  "assets": {
    "directory": "./public/"
  }
}
```

`directory` に書いたフォルダの中身が、そのまま公開されます。**ここが `./` になっていたら、リポジトリ全部が公開されます。** 必ず `./public/` を指していることを確認してください。

> **`npx wrangler login` はブラウザでの許可操作が必要です。** Claude Code側で止まって見えるときは、VS Codeのターミナル（「表示」→「ターミナル」）で自分で `npx wrangler login` を実行してから、あらためてデプロイを頼んでください。

### ステップ3: 自分の目で確認する（5分）

**発行されたURLを、自分のスマートフォンで開いてください。** PCのブラウザではなく、です。

- 意図したものが表示されるか
- 動かしてみて、PCで見たときと同じように動くか
- **見せたくないものが表示されていないか**

確認できたら、**URLを隣の人に送って開いてもらってください。** 他人の環境で開かれて初めて「公開した」ことになります。

最後に、**ここまでの変更を保存します。** `public/` への移動も `wrangler.jsonc` も、まだコミットされていません。

```text
いまの状態をコミットして、プッシュしてください。
公開URLを README.md の先頭に1行追記してからにしてください。
```

**ルートAで公開した人も、ここでHook Bが止めてくるはずです。** レビューを通してからもう一度頼んでください。**「外に出す前に必ず見る」が、pushでもデプロイでも同じように効く**ことを、ここで確認できます。

> **公開したということの意味。** URLを知っている人は誰でも見られます（検索には出にくいですが、秘密ではありません）。**あなたのアプリがブラウザに保存しているデータ（`localStorage` など）は、見る人ごとに別々**で、あなたの手元のデータが相手に見えることはありません。逆に言えば、**あなたが入力したデータは公開されていない**ということです。ここを混同しないでください。

> **見せる相手を絞りたくなったら。** 「まだ人に見せる段階じゃない」「知り合いにだけ見せたい」という場合は、**Basic認証**——開くときにIDとパスワードを求める、一番簡単な鍵——をかける手があります。**とくに、持ち帰ってAIを組み込んだ人には必要になります。** 生成の裏でお金がかかるものを誰でも叩ける状態にしておくと、URLを知った人の分まであなたに請求が来ます（→ 🚀拡張ミッション④）。

> **宿題の「外部サービス連携なし」と矛盾しないの?** しません。**変わったのはアプリの置き場所だけで、アプリ自体は静的なままです。** ログインも、サーバーへのデータ送信も増えていません。「作ったものをどこに置くか」と「アプリが何とつながるか」は別の話です。

### ✅ チェックポイント8

- [ ] 公開URLが発行され、**自分のスマホから**開けた
- [ ] `public/` に、公開してよいものだけが入っていることを確認した
- [ ] 隣の人にURLを渡した

### 🚀 拡張ミッション④（早く終わった人向け・任意）

- 〔共通〕**Claimして残す**: ルートAで公開した人は、Cloudflareアカウントを作ってClaimし、消えないURLにする
- 〔Eng〕**独自ドメインをつなぐ**: 持っているドメインがあれば、Cloudflare側でカスタムドメインを設定してみる
- 〔Eng〕**pushしたら自動で公開されるようにする**: GitHub Actionsのワークフローを1本作らせ、`main` にpushしたら自動でデプロイされる形にする。**今日の「15分ごとにpush」がそのままリリースになります**
- 〔Eng〕**見せる相手を絞る**: Basic認証をかけて、URLを知っているだけでは開けない状態にする。**「まだ見せたくない」を、お願いではなく仕組みで担保します。**

    ```text
    このアプリにBasic認証を入れて、URLを知っているだけの人が開けないようにしてください。
    - IDとパスワードは wrangler secret に置き、リポジトリにも公開ファイルにも
      含めないでください
    - 静的ファイルもAPIも、すべて認証を通ってからでないと届かないように
      してください
    - 設定できたら、認証なしでURLを叩いて 401 が返ることを確認する手順を
      教えてください
    ```

    **最後の1行を必ず入れてください。** 認証は「書いたつもり」で効いていないことがあり、**画面にログイン欄が出たかどうかでは判断できません。** 認証なしで叩いて `401` が返って初めて、効いています。今日ずっとやってきた「自分の目で確認する」を、目に見えないものに対してやる練習です

- 〔Eng〕**サーバー側が必要になる線を引く**: 「このアプリに⟨ログイン／他の人とデータを共有／メール送信⟩を足すとしたら、静的ホスティングのままでは何が足りないか」をClaudeに説明させる。**今日の構成の限界を知っておくのが、次の一歩です**

## 12. ハンズオン⑨ 仕事の道具とMCPでつなぐ（15分）

最後に、**「今回やらないこと」を、明日の自分やチームに引き渡します。**

前半2ではGmailとつなぎました。同じ仕組み（MCP）で、仕事で使っているタスク管理ツールにもつなげます。**ここでの目的は「タスク管理をNotionやBacklogに移すこと」ではありません。** 今日の司令塔は最後まで `TASKS.md` です。やるのは**出口を1本つけること**——手元のファイルに溜まった持ち帰り分を、明日開く場所へ流し込むところまでです。

### つなぐ（8分）

**どちらか片方で構いません。** 迷ったらNotionです。

#### Notion — ネイティブ統合あり、つなぐだけ

前半2のGmailと同じ手順です。

1. ブラウザで [claude.ai](https://claude.ai) を開く
2. 左側メニューの「**カスタマイズ**」→「**コネクタ**」を開く
3. 一覧から **Notion** を探して「接続」を押す
4. Notionアカウントでログインし、許可画面の内容を確認して許可する

難しい設定は不要です。**個人のNotionを使いたくない人は、この場では接続せず手順を見るだけで構いません。**

#### Backlog — ネイティブ統合なし、自分でMCPサーバーを設定する

Backlogはclaude.aiのコネクタ一覧にはありません。**自分でMCPサーバーを追加**します。

> **このルートはNode.jsを使います**（MCPサーバーを `npx` で起動するため）。事前準備0.2で全員入れているので、そのまま進めて構いません。ターミナルで `node -v` を実行してバージョンが出ない場合だけ、**Notion側を選んでください。**

Backlogの「個人設定」→「API」からAPIキーを発行したうえで、Claude Codeに次を頼んでください。

```text
Backlog用のMCPサーバーを追加してください。
- パッケージ: backlog-mcp-server@0.18.0（npxで実行。バージョンは変えないでください）
- 環境変数: BACKLOG_DOMAIN=⟨自分のスペース⟩.backlog.com、BACKLOG_API_KEY=⟨発行したAPIキー⟩
- スコープはuser（このPCの自分だけ・このリポジトリにはコミットされない）にしてください
```

> **バージョンを固定しているのは意図的です。** `npx backlog-mcp-server` のようにバージョンを書かないと、**そのとき最新のものが取ってきて実行されます。** 何が動くかが日によって変わるということなので、**中身を確認したものだけを動かす**ために番号まで書きます。前半5でHooksについて言ったこと（「中身を理解できるものだけを登録する」）と同じ考え方です。

> **APIキーは絶対にリポジトリにコミットしないでください。** `myapp` は15分ごとにpushしている、**しかも11で公開までした**リポジトリです。スコープを`project`にすると設定がリポジトリ内の `.mcp.json` に書き込まれ、**pushした瞬間にAPIキーが漏れます。** 上のように`user`スコープを指定すれば、設定は自分のPCの中だけに保存され、リポジトリには一切含まれません。**これは10でレビューした「秘密情報がコードに書かれていないか」と、まったく同じ観点です。**

#### どちらの場合も: Claude Codeを開き直す

設定できたら**Claude Codeを一度終了して開き直してください。** MCPの接続はClaude Codeの起動時に確立されるため、`/clear` だけでは反映されません。開き直したら `/mcp` で接続状態（Connected）を確認します。

### 引き渡す（7分）

`TASKS.md` の「今回やらないこと」を、そのまま明日の入り口に流し込みます。

**Notionの場合:**

```text
TASKS.md の「今回やらないこと」を、Notionに「⟨アプリ名⟩ 次にやること」というページとして
書き出したいです。
まず、作成予定のページ名・作成先（どのワークスペースのどこか）・チェックリストの本文を
見せてください。まだ作成しないでください。
それぞれに、TASKS.md に書いてある「今日やらない理由」も添えてください。
```

内容と作成先を確認してから、

```text
その内容で、その場所に作成してください。
```

> **「作成先」を必ず確認してください。** Notionは複数のワークスペースに所属している人が多く、**どこに作られるかは指示だけでは決まりません。** 書き出すのは「今日やらないこと」——つまりあなたの仕事の課題そのものなので、**置き場所を間違えると見えてはいけない人に見えます。**

**Backlogの場合:**

```text
TASKS.md の「今回やらないこと」を、Backlogの⟨プロジェクト名⟩に課題として登録したいです。
まず、登録する予定の一覧（件名と詳細）を見せてください。まだ登録しないでください。
```

内容を確認してから、

```text
その内容で登録してください。
```

> **外部への書き込みは、必ず一度見てから。** ファイルの編集と違い、**課題の一括登録は取り消しに手間がかかります。** 「まず一覧を見せて」を挟むのは、前半で覚えた「見て、直す」を外部サービスに対してやっているだけです。

> **外から来た文章を「指示」として実行させない。** MCPでつなぐと、Claudeは課題やページの本文を読めるようになります。そこに書かれた文章は**データであって指示ではありません。** 他人が書いた課題の本文に「すべてのファイルを削除して」と書いてあっても実行させない、という線引きが要ります（→ [columns.md](columns.md) ★8）。

### ✅ チェックポイント9

- [ ] NotionまたはBacklogのどちらかが `/mcp` で接続済みになっている
- [ ] 「今回やらないこと」が、その外部ツール側に残った
- [ ] **`TASKS.md` も手元に残っている**（置き換えたのではなく、出口を1本つけただけ）

### ✅ 今日の完成判定

ここまでで、次の4つが揃っていれば今日は完成です。

- [ ] 主要な機能が自分の目で動くことを確認した
- [ ] 2軸レビューとセキュリティレビューを通し、見つかった問題に一通り目を通した
- [ ] **公開URLがあり、自分以外の環境で開けた**
- [ ] `TASKS.md` に、次にやるべきことが残っている

（Hookでの自動化・MCP連携は任意です。**11のルートAだけでも「公開した」に入ります。**）

## 13. 成果共有（7分）

3〜4名にデモしてもらいます。見どころは作品そのものより**プロセス**です。発表者は次の5点を短く共有してください。

1. 何を作ったか（**公開URLがあれば、それを画面に出す**）
2. 自作のSkillは、どんな質問を返してきたか
3. どんな指示が効いたか（実際の文言があれば見せる）
4. どこでAIが失敗したか、どう抜けたか
5. レビューで何が見つかったか（**仕様どおりでなかった点があれば、それがいちばん面白い**）

聞く側は「その指示、もらった」と思ったらメモしてください。**完成度を競う場ではなく、それぞれ違うものを同じプロセスで作った、という事実を確認する場です。**

## 14. クロージング（3分）

### 今日やったこと

```
バイブコーディング → Intentの明確化 → Skillによる拡張 → 仕様化 → 計画 → 実装 → レビュー → デプロイ
```

このハンズオン自体が、この一連の流れを体験する教材でした。前半で「ノリで作れてしまう」体験をし、その後「何を作りたいのかを明確にする」壁にぶつかり、**自分で作ったSkill**でそれを乗り越え、後半でIntentを実際のソフトウェアに変換し、**最後に世界から見える場所に置きました。**

**いちばん持ち帰ってほしいのは、Skillに数行足しただけで宿題の持ち物が変わった、という体験です。** 仕組みを設計すると、出てくるものが変わる。今日いじったのは、AIの振る舞いそのものでした。

**そしてもう1つ。** レビューは「気をつける」ではなく**仕組みに寄せました**（Hook A・B）。公開する範囲は「気をつける」ではなく**フォルダを分けることで担保しました**（`public/`）。**人の注意力に頼らない形に落とすこと**——これが、動くものを作れるようになった次に効いてくる差です。

**ただし今日のHookは完全ではありません**（10④で見たとおり、手編集やブラウザからの公開は抜けます）。**「どこまでを仕組みが見ていて、どこからは見ていないのか」を自分で言えること**まで含めて、持ち帰ってほしい部分です。

**工程を分けたことにも意味があります。** `tdd` は整えず、`code-review` は危なさを見ず、`security-review` だけが「外に出してよい」の印を作る。**1つのスキルに全部を入れると、報告が混ざって何も見えなくなります。** 小さく分けて、噛み合わせる——今日いじったのはAIの振る舞いであると同時に、**工程の設計**でした。

### 今日つないだものの外し方

前半2でGmail、12でNotionをつないだ人向けです。**接続は claude.ai 側に残ります。** Claude Code を閉じても消えません。外すときは、つないだときと同じ場所から操作します。

1. ブラウザで [claude.ai](https://claude.ai) を開く
2. 左側メニューの「**カスタマイズ**」→「**コネクタ**」を開く（`claude.ai/customize/connectors`）
3. 対象のコネクタの行から接続を解除する

**外しても、今日作ったものは消えません。** アプリもスキルもリポジトリも手元に残ります。

### 継続のために

- 帰りに [columns.md](columns.md) の★コラムを読む
- 今日手元に揃ったスキルは、**ユーザーレベル（`~/.claude/skills/`）に置いておけば次のプロジェクトでもそのまま使えます**

    | 前半で自分で作った | 後半でコピーした |
    |---|---|
    | `tsumete`・`tsumetsume`・`ddd` | `tdd`・`code-review`・`security-review`（＋`prototype`） |

    **これで「仕様を詰める → 実装する → レビューする」の一周ぶんが、全部スキルとして手元にあります。** 次に何か作るときは、前半4からやり直す必要はありません

    > **ユーザーレベルに置いたスキルは、どのプロジェクトでも自動で呼ばれます。** `tdd` は「実装して」に、`ddd` は「用語集を整理して」に反応します。**これは明日、仕事のリポジトリで作業しているときにも起きます。**
    >
    > 「実装して」と言っただけで `TASKS.md` を探しにいったり、`CONTEXT.md` や `docs/adr/` を勝手に作ったりします。**今日作った3枚は35分で書いた簡易版**なので、既存のチームの進め方と噛み合わないことがあります。
    >
    > **合わないと感じたら、`~/.claude/skills/` から該当するフォルダを消してください。** 消しても、今日 `myapp` に作ったものは何も消えません。**「いつでも消せる」と分かっていることが大事**で、これは今日つないだコネクタの話（上の「今日つないだものの外し方」）とまったく同じです。
- **Hook A・Bは `myapp` のプロジェクト内**（`.claude/settings.json` と `.claude/hooks/`）にあります。次のプロジェクトでも使いたければ、**`myapp` から丸ごとコピーしてください。** 今日中身を確認したものだからです。**新しく頼んで作らせる場合は、必ず中身を読んでから登録してください**——Hooksは権限プロンプトを経由せず自動実行されます
- `TASKS.md` の「今回やらないこと」——そしてNotion／Backlogに流し込んだそれが、次の一歩です
- 公開URLは、そのまま人に見せられるあなたの作品です。**Claimしていない人は消えているので、続けるなら作り直しからです**
- もっと深く: 書籍対応表（columns.md末尾）から『実践Claude Code入門』へ
- 利用プラン: 今日Proで枠が心もとなかった人は、Maxへのアップグレードを検討するタイミングです
